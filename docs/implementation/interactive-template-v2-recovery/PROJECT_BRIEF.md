# 交互模板 v2 终局恢复与上线项目简报

- 状态：计划 v3.0 已批准，P03R 文档重基线进行中
- 工作区与仓库：`/Users/rosswang/Desktop/HTML - PPT/03_HTML汇报模板资产管理与组装平台`
- 实施分支：`feat/interactive-template-v2-export`
- 实施基线：`bdd9acf35174fefe7953aa59c5d7bb670d4cf901`
- 使用者：PocketBay 模板资产库管理员、云服务器管理员与已登录成员

## v3.0 目标与成功标准

- `ppt.ajjy-ai.site` 与 PocketBay 使用同一精确发布 SHA 和三栏资产库形态：左侧分类筛选、中间真实缩略图、右侧已选汇报。
- 点击预览后打开大尺寸 16:9 弹窗；v1 显示真实 HTML/CSS，v2 显示可点击、可拖拽、可重播和重置的隔离交互页面。
- 先完成 PocketBay 恢复门禁和模板迁移，再从 PocketBay 导出唯一权威目录包并同步到云服务器。
- 仅同步模板资产、标签、版本、current 指针、内容对象、预览图和 renderer identity。
- 云服务器现有用户、密码、Session、汇报、汇报项目和导出历史均不进入传输包，且同步前后内容指纹必须不变。
- 云端独有活动模板仅软退役，不删除版本或 CAS；历史汇报继续解析其固定版本。
- PocketBay 成功目标为 27 个活动模板：15 个 Workshop 加 12 个 current v2；三个既定合同模板软退役。

## 范围与授权

- P03R 仅物化 v3.0 父监督链文档，不修改产品源码、测试、ops、生产或远端，不启动 P04/P05A/P05B/P06。
- 已批准的后继范围依序为：P04 本地实现与验证、P05A PocketBay 跨版本密封恢复证明、P05B PocketBay 业务迁移与目录导出、P06 云服务器部署和目录应用。
- P06 的唯一云服务器目标为 `119.29.241.146`，唯一公开 Origin 为 `https://ppt.ajjy-ai.site`；只有所有前置门禁通过后，父级才可在该目标执行计划内操作。
- 后继生产操作必须使用精确冻结 SHA、Node `22.23.2` 和 pnpm `9.15.0`，先构建 shared，再构建 API/Web。
- PocketBay 登录、备份口令及云服务器特权认证由用户手工完成；任何 worker 不读取、记录或输出口令、Cookie、token 或密钥。

## 固定安全边界

- `.workbuddy/` 保持不读、不改、不暂存。
- 不读取或改写 `01_AI协会_Workshop_HTML`、`02_HTML_PPT_组件与模板`；12 个 v2 仅来自仓库内确定性生成器。
- 不删除、移动或覆盖旧备份、CAS、模板版本或任何既有数据；不自动升级历史 PresentationItem。
- 任一备份、恢复、目录指纹、真实交互或线上验收门禁失败时停止，保持维护/只读状态，不自动回滚或越过门禁。

## 已核实基线

- P00、P01、P02 的既有 passed 证据保持不变。
- 旧计划 v2.1 的 P03A 未通过、P03B 未启动；二者作为被 v3.0 取代的历史记录保留。
- v3.0 活动链为 `P03R → P04 → P05A → P05B → P06`，终局阶段为 P06，后继阶段由父级独占启动。
