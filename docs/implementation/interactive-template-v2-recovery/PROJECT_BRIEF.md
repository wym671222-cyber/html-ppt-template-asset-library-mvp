# 交互模板 v2 终局恢复与上线项目简报

- 状态：计划 v2.1 已批准并执行
- 工作区与仓库：`/Users/rosswang/Desktop/HTML - PPT/03_HTML汇报模板资产管理与组装平台`
- 实施分支：`feat/interactive-template-v2-export`
- 基线：`a7437024d471f6f4679202824ddd30a060642055`
- 使用者：PocketBay 模板资产库管理员与已登录成员

## 目标与成功标准

- 保持当前 `v788`，修复 PocketBay 版本切换会破坏裸 SQLite 备份物理完整性的问题。
- 新备份使用原子密封 ZIP；旧有效目录保持兼容，旧无效目录保持可见但不可使用。
- 两次生产版本切换后同一密封归档字节不变且可隔离恢复。
- 线上最终为 27 个活动模板：15 个 Workshop、12 个 current v2 交互组件；三个合同模板软退役。
- 历史 revision 18 汇报继续固定四个 v1 项；交互与离线导出保持断网安全。

## 范围与授权

- 范围内：恢复服务、恢复 API/UI、仓库内测试、同一 PocketBay 项目的两次候选部署、备份、恢复演练、12 项 v2 晋升、3 项软退役和终局验收。
- 允许：向 `origin/feat/interactive-template-v2-export` 推送精确候选；在当前 PocketBay 项目创建新版本和计划内业务写入。
- 禁止：读取或改写两个真实来源目录、删除旧备份/CAS/版本、自动升级历史 PresentationItem、操作其他项目或腾讯服务器链。
- 口令、Cookie、token、密钥不得读取、记录或输出；备份口令由用户在 Ego Lite 中手工输入。

## 当前证据

- 旧链 P00–P06 已通过；P07 在 `v788` 部署后因备份 SQLite 哈希/尺寸失配停止，模板写入为零。
- PocketBay 控制台显示历史版本无数据库副本，镜像回滚不能修复 `/data/recovery-backups`。
- 当前分支与远端均为 `a743702`；工作树仅有既有未跟踪 `.workbuddy/`。

## 开放决策

- 无。计划 v2.1 与“保留 v788”路线已由用户明确批准。
