# P04：机械拆分记录

## 结果

- 原 P04 worker 在约 55–60% 上下文返回 `split_required`；未提交、未启动后继，也未宣称测试或构建通过。
- runtime 草稿与 transfer 草稿均保留，但都属于未验证工作，不能作为门禁证据。
- `git diff --check` exit `0`；主链基线仍为 `e7d6189728ded87dbccffce38bb191aeac935bd3`。

## 机械拆分

- P04A：完成 v1/v2 runtime 判别联合、大尺寸弹窗、全屏/适应窗口/Esc/焦点恢复、iframe 销毁和真实浏览器验收。
- P04B：完成密封目录包、管理员 API/BFF、只读保护、事务/敏感表指纹及全部负测。
- 该拆分不改变 v3.0 批准目标、范围、模型类别、生产授权或终局验收；执行计划版本增为 3.1，批准版本保持 3.0。

## 边界

- P04A 通过父级独立门禁前不得启动 P04B。
- P04B 通过前不得启动 P05A。
- 两阶段均禁止生产、远端、真实素材目录和 `.workbuddy/` 操作。
