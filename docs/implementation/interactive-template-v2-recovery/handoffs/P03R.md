# P03R：v3.0 恢复链重基线

## Worker 结果

- 计划版本与批准版本均固定为 `3.0`，批准状态为 true；实施基线更新为 `bdd9acf35174fefe7953aa59c5d7bb670d4cf901`。
- P00–P02 的 passed 提交与门禁证据保持不变；P02 后继改为 P03R。
- 旧 P03A 保留为未通过的 `in_progress` 历史，旧 P03B 保留为未启动的 `pending` 历史；没有伪造 passed。
- 新活动链为 `P03R(in_progress) → P04(pending) → P05A(pending) → P05B(pending) → P06(pending)`，terminal 为 P06。
- 新阶段模型统一为 `gpt-5.1-codex-max / high`，上下文预算依次为 20% / 50% / 45% / 50% / 50%。
- v3.0 的预览、目录传输、PocketBay 先行、固定云端、数据保护、恢复和失败停止合同已写入恢复链文档。

## 改动边界

本 worker 只修改以下恢复链文档：

- `PROJECT_BRIEF.md`
- `DECISION_LOG.md`
- `MASTER_PLAN.md`
- `STATUS.md`
- `CHAIN_STATE.json`
- `handoffs/P03R.md`

未修改产品源码、测试、ops 或旧 `docs/implementation/interactive-template-v2/**`；未访问生产、远端、`.workbuddy/` 内容或两个真实素材目录；未删除、移动或覆盖既有数据。

## 本阶段门禁

- 官方 chain validator：提交前必须通过。
- `git diff --check`：提交前必须通过。
- 只显式暂存上述六个文件并审查 staged diff；禁止 `git add -A`。
- 形成且仅形成一个 Conventional Commit。
- 提交后 tracked worktree 必须干净，只允许保留既有 `?? .workbuddy/`。

## 父级待验收清单

- 从 P03R 精确提交独立确认六文件边界、分支、基线和 v3.0 批准记录。
- 独立复跑官方 validator 与 `git diff --check`，核对 P00–P02 证据未变。
- 确认旧 P03A/P03B 仅在历史区，分别为未通过和未启动，没有 passed。
- 确认 P04/P05A/P05B/P06 均为 pending，且 threadId/commit 均为 null。
- 父级验收通过后才可把 P03R 标记 passed 并决定是否创建 P04；本 worker 没有创建、启动、指派或实现 P04。
