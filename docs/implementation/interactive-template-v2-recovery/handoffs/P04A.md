# P04A：实时预览与大尺寸弹窗

## 交付状态

- 计划版本：`3.1`；用户批准版本：`3.0`
- 阶段：P04A — 实时预览与大尺寸弹窗
- 线程：`/root/p04a_live_preview_ui`
- 模型合同：`gpt-5.1-codex-max / high`
- 起始协调 SHA：`f684b1ad8392`
- worker commit：`966bcf95c2e28663286a2dd08f244000b1ae8aa0`
- worker gate result：`passed`；parent gate result：`passed`

本阶段只完成本地 runtime、弹窗 UI、BFF 安全边界和验收测试。父级已独立复核并将 P04A 标记为 `passed`；后继仅允许进入 P04B 本地目录传输实现。

## 交付范围

- 共享目录 runtime 联合为 `sandboxed-static | sandboxed-js`，固定 `1920×1080` viewport、同源 runtime URL 和精确 commands；v1 commands 为空，v2 仅 `replay/reset`。
- v1 current 包经过既有静态预览策略、摘要和模板身份复核后，生成真实 DOM/CSS runtime；声明 CSS 内联，未解析 link、脚本、外部资源、导航或下载均拒绝。
- API/BFF 只转发匹配 mode 的精确 CSP/header 合同：v1 无 protocol/session，v2 必须有 nonce-bound CSP、protocol/session；额外上游 header 不跨边界。
- 资产卡片保持 PNG-only。预览弹窗居中、16:9、实际页面优先、元数据默认收起；提供适应窗口、全屏和关闭，v2 额外提供重播/重置。
- Esc 在真实全屏时先退出全屏，后续 Esc 关闭；关闭后 iframe 从 DOM 销毁并把焦点归还原眼睛按钮。

## 固定运行时与门禁

所有通过证据均使用 Node `v22.23.2`、pnpm `9.15.0`，并将固定 Node bin 置于 `PATH`，确保 pnpm 启动的 `tsc`、Vitest、Vite 和 Playwright 子进程不回落到系统 Node。

| 门禁 | 字面结果 |
|---|---|
| Runtime | `pnpm exec node --version` → `v22.23.2`；`pnpm --version` → `9.15.0`；exit `0`。 |
| Shared build | `pnpm --filter @slide-maker/shared build` → exit `0`。 |
| 聚焦 Vitest | `pnpm exec vitest run tests/p04a-live-preview.test.ts tests/p02-interactive-runtime.test.ts tests/p06-asset-library.test.ts tests/p15-bff.test.ts` → exit `0`；`4/4 files、23/23 tests`。 |
| Web check | `pnpm --filter @slide-maker/web check` → exit `0`；`0 errors / 9 warnings`，warning 均来自未修改既有文件。 |
| API/Web build | `pnpm --filter @slide-maker/api build && pnpm --filter @slide-maker/web build` → exit `0`；API TypeScript 与 Web adapter-node production build 通过。 |
| Google Chrome P04A/P06 | `pnpm exec playwright test --config playwright.p06.config.ts e2e/p06-asset-library.spec.ts` → exit `0`；`8/8`。 |
| Google Chrome 12 v2 | `pnpm exec vitest run tests/p05-interactive-fixtures.test.ts -t "uses a sandboxed offline document and proves every component changes its own observable state"` → exit `0`；过滤后 `1 passed / 3 skipped`，该测试内部逐项覆盖 12 个 v2。 |

首次聚焦 Vitest 在未固定 pnpm 子进程 `PATH` 时真实 exit `1`，唯一失败为 `better-sqlite3` ABI 127/137 环境不匹配；没有产品断言失败。修正运行时后从头重跑并得到上表通过结果，不把首次失败误报为产品回归或通过证据。

## 浏览器证据

- Target flow：`http://127.0.0.1:5175/` → 点击卡片眼睛按钮 → 大尺寸实际 runtime → v1/v2 控件、关闭与焦点状态。
- 应用内 Browser：页面标题 `HTML 汇报模板资产库`、DOM 非空、无 framework overlay、console error/warn `0`。
- 1440×900：workspace columns `280px 770px 390px`，asset grid 两列 `356.5px 356.5px`，document `1440×900`；1920×1080：workspace columns `280px 1250px 390px`，asset grid 两列 `596.5px 596.5px`，document `1920×1080`。两者左筛选、中卡片、右汇报可见，卡片 iframe `0`。
- v1：实际 `Quarterly brief title` DOM/CSS 可见，iframe `sandbox=""`、`referrerpolicy="no-referrer"`、16:9、元数据未展开，重播/重置按钮数 `0`。
- v2：iframe `sandbox="allow-scripts"`，内层真实页面可见；重播/重置后状态为 `replay:1` / `reset:2`，适应窗口 pressed 状态可切换。
- 关闭：dialog 和 iframe 均变为 `0`，active element 的 aria-label 为对应 `预览 …` 按钮。
- 应用内 Browser 容器未暴露 Fullscreen API，不能作为真实全屏证据；未伪造通过。指定本机 Google Chrome 的 E2E 已验证全屏进入、退出按钮及两步 Esc 合同。

## 安全边界与父级复核

- v1 iframe 无 `allow-scripts`；v2 iframe 精确只有 `allow-scripts`，无 `allow-same-origin`、下载、导航、外网或宿主存储授权。
- 列表不运行 iframe；runtime 仅在弹窗打开时创建，关闭即销毁。
- 未接触 P04B 临时草稿、生产、PocketBay、云端、远端、凭据、`.workbuddy/` 或两个真实素材目录。
- 父级应从本提交独立复跑关键门禁、审查 staged/commit 边界和工作树，再决定是否将 P04A 标为 passed；worker 不启动 P04B。

## 父级独立门禁

- 父级在固定 Node/pnpm 下独立复跑 Shared、23/23 聚焦测试、API/Web build、Web check、Google Chrome `8/8` 与 12 个 v2 真实交互门禁，全部 exit `0`。
- 父级应用内 Browser 复核两个桌面视口、三栏双列、v1/v2 实际页面、零卡片 iframe、零横向溢出和零 console error/warn；真实全屏和 Esc/焦点合同以指定 Google Chrome 的直接 E2E 为准。
- validator、diff check 与提交后工作树门禁通过，仅保留既有 `?? .workbuddy/`；P04A 最终 parent gate 为 `passed`。
