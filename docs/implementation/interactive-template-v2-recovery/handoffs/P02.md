# P02：候选与独立门禁

## 交付状态

- 计划版本：`2.1`
- 阶段：P02 — 候选与独立门禁
- 线程：`/root/p02_candidate_gate`
- 模型合同：`gpt-5.6-sol / xhigh`
- 起始协调 SHA：`09c192f6f2d40ec2c1b90b066bbdf42493a7aadf`
- 产品候选 A：`87ba2e0373f749d9198d38bde9020703b282c703`
- 候选 B：本 handoff 与 `STATUS.md` 所在的 docs-only 原子提交
- worker gate result：`passed`；parent gate result：`pending`

本阶段只执行本地候选门禁并写入新恢复链证据，没有修改产品、运行时、部署输入或 `CHAIN_STATE.json`，没有创建或启动 P03A，没有 push、部署或操作生产、其他项目、旧链、`.workbuddy/` 或两个真实来源目录。

## 候选 A/B 一致性

- 从候选 A 到起始协调 SHA 的完整差异仅有 `docs/implementation/interactive-template-v2-recovery/**`；本阶段相对起始协调 SHA 仅新增本 handoff 并更新同链 `STATUS.md`。
- 对候选 A 与候选 B 比较 `Dockerfile`、根和 workspace package manifests、`pnpm-lock.yaml`、`pnpm-workspace.yaml`、`turbo.json`、`apps/**`、`packages/**`、`fixtures/**`、`ops/pocketbay/**`、`templates/**`，逐字 diff 为空。
- 候选 B 因 docs-only 门禁证据形成不同 PocketBay 版本输入，但运行时与部署代码和候选 A 完全相同；父级须从提交后的精确 B SHA 独立复核该结论。

## 固定运行时与完整门禁

所有正式命令使用隔离 Node `v22.23.2`、独立 Corepack home 和 pnpm `9.15.0`；`pnpm exec node --version` 精确为 `v22.23.2`。

| 门禁 | 结果 |
|---|---|
| 全量 Vitest | exit `0`；`45/45` files、`835/835` tests。 |
| Shell | exit `0`；`8/8` suites（`90/90`、`77/77`、`103/103`、`68/68`、`56/56`、`18/18`、`31/31`、`14/14`）。 |
| Shared build | TypeScript build exit `0`。 |
| API build | TypeScript build exit `0`。 |
| Web check | exit `0`；`0 errors / 9 warnings`，均为既有未修改文件。 |
| Web build | adapter-node production build exit `0`；既有 Svelte/Rollup warning 保留。 |
| Root Turbo build | shared/API/Web `3/3` successful；exit `0`。 |
| 真实 Google Chrome 主套件 | `12/12`：P06 catalog `5/5`、P07 presentation `2/2`、P08 offline `3/3`、P09 recovery `2/2`。 |
| 真实 Google Chrome 补充 P02 | `1/1`；全量 Vitest 中 P02 unrestricted second-listener fetch/self-navigation `0-hit` 与伪造命令拒绝同时通过。 |

P02/P06 专用端口 `3017/5174/3018/5175` 在对应套件启动前均为 `0` listener，Playwright 退出后均释放。测试只使用仓库内 fixture、临时 SQLite/CAS 和本机回环端口。

## 依赖审计

- `pnpm audit --prod --json`：真实 exit `0`，production dependencies `370`；metadata `info=0`、`low=0`、`moderate=0`、`high=0`、`critical=0`。
- 字面补充 `pnpm audit --json`：真实 exit `1`，dependencies `638`；metadata `info=0`、`low=2`、`moderate=6`、`high=1`、`critical=1`。
- 完整依赖审计的非零结果原样保留；本阶段没有使用 ignore、`--force`、降阈值、approve-builds、override 或依赖升级。批准门禁要求的 production audit 保持 exit `0` 且五级全零。

## 安全、路径与固定边界

扫描只基于候选 B 的 `544` 个 tracked files，跳过符号链接读取，不纳入未跟踪 `.workbuddy/`，不遍历仓库外 sibling 目录；只记录计数和路径类别，没有输出匹配值。

- high-confidence token format：`0`；private-key marker：`0`。
- credential-like assignment：全 tracked tree `22` 处、`6` 个路径，类别为 test `4`、QA tooling `2`；deployable scope 为 `0`。
- absolute user path：全 tracked tree `36` 处、`25` 个路径，类别为 documentation `22`、QA tooling `2`、other tracked `1`；deployable scope 为 `0`。
- deployable scope 中两个禁止真实来源目录名为 `0`。
- `.workbuddy/` tracked count `0`、staged count `0`；本阶段未读取其文件内容、未修改、未暂存。
- 未读取、扫描、导入、复制、移动或改写 `01_AI协会_Workshop_HTML`、`02_HTML_PPT_组件与模板`；未访问生产 DB/CAS、PocketBay 控制面、腾讯服务器链或其他远端。

## 父级复核与回滚

- P02 阶段账本继续保持 `in_progress / pending`，`CHAIN_STATE.json` 未改；父级应独立复跑关键门禁、回读精确 B SHA、审查 A..B 分类、打包并决定是否 push 及启动 P03A。
- 若父级拒绝本阶段，只对本次 docs-only 候选 B 执行 `git revert <P02-B-SHA>`；不得 destructive reset、删除 `.workbuddy/` 或改写 P01 产品提交。
