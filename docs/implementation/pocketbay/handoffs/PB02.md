# PB02 交接：加密备份与恢复

- Product commit：`64c6fbb3b968322a93859158602d11a9c04a6d3e`
- Gate：passed
- 后继阶段：PB03 管理员模板导入

## 已交付

- 备份可导出为最多 128 MiB、5,002 个受控文件的 stored ZIP；导入逐文件核验路径、CRC、SHA-256、manifest、migration、SQLite 和 CAS。
- 浏览器以 PBKDF2-SHA-256（600,000 次）派生 AES-256-GCM 密钥，生成 `.pba`；口令不发送到 API，成功后从页面状态清除。
- 导入后必须先执行隔离恢复；旧会话在恢复副本中被撤销。
- 激活需要输入包含备份 id 的精确确认串，只写 activation request；下次进程启动在数据库打开前复核并原子切换，原数据库保留在 rollback 目录。
- 失败或中断时恢复旧数据库并失败关闭；不会在运行中覆盖打开的 SQLite。

## 验证证据

- 加密与恢复定向测试：2 files / 12 tests passed（Node 22）。
- 全量 Vitest：38 files / 794 tests passed（Node 22）。
- API typecheck、Web check（0 error/9 existing warnings）、根构建和 shell 8/8 通过。
- 生产依赖审计所有 severity 为 0 且退出码 0。
- 暂存路径与凭据字面量扫描通过；未纳入 `.workbuddy/`、数据库、CAS 或真实素材。

## PB03 边界

- 仅接受管理员显式上传的 html-template/v1 ZIP；不扫描或导入工作区外素材。
- 文件数、压缩后/展开后大小、路径、类型和 HTML/CSS 能力必须严格 allowlist。
- 先注册 CAS 和 catalog，再通过单并发受限 worker 生成 preview/thumbnail；失败模板不可进入可用目录。
