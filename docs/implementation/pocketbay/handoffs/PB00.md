# PB00：固化最小适配基线

- 状态：passed
- 计划版本：1.0
- 模型与推理：gpt-5.6-sol / high
- 起点：`450d9d500088aed82176c10bd7bf44de18edcc6c`
- 产品提交：`b30967fb02da1cc770425bb68017f448e430a7d9`

## 完成内容

- 增加 PocketBay Dockerfile、单容器双进程启动器和 `/data` 持久化边界。
- Web 对外绑定平台 `PORT`/`0.0.0.0`，API 仅监听 loopback。
- 固化当前 PocketBay Origin 适配和一次性管理员初始化读取路径；配置文件不进入镜像。
- 新建独立 PocketBay 执行链；旧 P17 文件和状态未修改。

## 验证

| 检查 | 结果 |
|---|---|
| PocketBay adapter Vitest | 6/6 passed |
| 全量 Vitest（Node 22.23.2 / ABI 127） | 36 files / 785 tests passed |
| shared/API TypeScript | passed |
| Web check/build | 0 errors；9 个既有 warning；build passed |
| root build | API/Web 2/2 passed |
| shell | 8/8 suites passed |
| Docker 本机构建 | Docker daemon 未运行；未执行，转交 PB04 平台构建门 |
| 范围扫描 | 未暂存 `.workbuddy/`、sibling、DB/CAS、真实素材或凭据 |

## PB01 入口

- 先读 `apps/api/src/env.ts`、`apps/api/src/app.ts`、`apps/api/src/routes/auth.ts`、Web hooks/BFF/登录注册页面和 `ops/pocketbay/start.mjs`。
- 只做精确 Origin、注册关闭、常规生产配置移除 bootstrap；不得提前实现备份或模板导入。
