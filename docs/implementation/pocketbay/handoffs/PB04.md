# PB04 受控上线与终局验收交接

- 阶段：PB04
- 状态：passed
- 产品提交：`ae5f4d4a7a3c603cf41a882f911667fd190a707f`
- 分支：`feat/pocketbay-production`
- 完成时间：2026-08-14

## 结果

当前项目已部署到现有 PocketBay 项目：

`https://html-ppt-template-asset-library.pocketbay.app`

最终成功条件已满足：PocketBay 返回 `next_action=done`、`project_status=running`，Release 235；Dockerfile 构建成功，单容器健康检查监听 8080 通过。

## 最小修复

首次候选 Release 234 的运行日志显示 API 因 `@slide-maker/shared` 生产导出仍指向 TypeScript 源文件而报 `ERR_MODULE_NOT_FOUND`，公网返回 502。

仅做两处修复：

- `packages/shared/package.json` 增加 `tsc` 生产构建，并将 package exports 指向 `dist/index.js` / `dist/dnd-transforms.js`。
- `Dockerfile` 在 API/Web 构建前先执行 shared package build。

未改变业务逻辑、认证策略、数据路径、模板契约或生产部署边界。

## 本地证据

| 检查 | 结果 |
|---|---|
| shared/API/Web 定向构建 | 通过 |
| 根 Turbo build | 3/3 packages successful |
| 全量 Vitest | 39 files / 798 tests passed |
| shell 回归 | 8/8 suites passed |
| Web check | 0 errors / 9 existing warnings |
| 生产依赖审计 | exit 0；info/low/moderate/high/critical 全 0 |
| 本地 API/Web 运行冒烟 | API live/ready 200；Web BFF live 200；API/Web 无模块缺失错误 |

本地 PocketBay 运行时完整启动需要 `/usr/bin/chromium`；本机未安装该路径，因此未伪造本地 Docker 运行结果。生产 Dockerfile 已安装 Chromium 和 CJK 字体，平台构建日志确认步骤完成。

## 线上证据

- 配对会话已创建并由用户在网页确认；凭据未进入对话、源码、Git 或部署包。
- 部署包基于提交归档，497 files；排除 `.env`、数据库/dump、私钥、凭据、管理员 bootstrap 配置、`.workbuddy/` 和 sibling `02_HTML_PPT_组件与模板`。
- PocketBay Release 235：Docker 构建、推送、数据同步、`/data` 挂载、8080 健康检查和切换均成功。
- 真实浏览器首次访问显示 PocketBay 休眠唤醒页；点击“唤醒并继续”后进入应用登录页，页面标题为“登录 · 模板资产库”。
- 唤醒后公网：`/` 303、`/login` 200、`/api/health/live` 200、`/api/health/ready` 200；登录页包含 CSP、`nosniff`、`SAMEORIGIN`、`no-referrer` 安全响应头。
- 动态应用约 10 分钟无真实访问后可进入休眠；再次访问时需在 PocketBay 页面确认“唤醒并继续”，这是平台生命周期行为，不是应用故障。

## 未覆盖项与交接动作

- 未在自动化浏览器中输入管理员新口令；用户已完成口令轮换，需由用户本人在登录页完成一次登录确认。
- 公开命令行 POST 注册探针受 PocketBay 外层代理返回 502，因此不把该一次探针写成应用层 403；注册关闭已有源码、测试、部署环境和登录 UI 证据。
- 后续按 `RECOVERY_RUNBOOK.md` 做一次真实隔离恢复演练；不得上传真实素材或本地业务 DB/CAS。

## 边界确认

- 未读取、修改或上传 `.workbuddy/`。
- 未读取、扫描、复制、移动、改写或上传 sibling `02_HTML_PPT_组件与模板`。
- 未上传本地 DB/CAS、真实素材或任何凭据。
- 未修改旧 P17，也未创建 P18。
