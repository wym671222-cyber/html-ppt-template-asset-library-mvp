# PB03 交接：管理员模板导入

- Product commit：`530e37bd48ab331cca9c70c2f1f2db0535dd2fe2`
- Gate：passed
- 后继阶段：PB04 受控上线与终局验收

## 已交付

- 管理员页面支持显式上传 `html-template/v1` ZIP 并轮询预览 Job。
- API 限制压缩包 5 MiB、展开 10 MiB、声明文件 32 个；拒绝路径逃逸、符号链接、未知字段、未声明文件、非 UTF-8、脚本/外部资源和不安全 HTML/CSS。
- 规范化模板源码登记到 CAS 与 catalog，同一不可变包重复上传复用确定性 Job。
- 单并发 worker 以 JavaScript 关闭、空 cookie、loopback 请求 allowlist 的 Chromium 生成 1280×720 和 320×180 PNG。
- 只有同 renderer 完整 PNG 对落库后模板才出现在资产目录；失败任务保持隐藏。
- Docker 安装系统 Chromium 与 CJK 字体，并固定生产可执行路径。

## 验证证据

- 导入/预览定向测试：4 files / 20 tests passed；本机 Chrome 集成包含在 P05 gate。
- 全量 Vitest：39 files / 798 tests passed（Node 22）。
- API typecheck、Web check（0 error/9 existing warnings）、根构建与 shell 8/8 通过。
- 生产依赖审计所有 severity 为 0 且退出码 0。
- 精确暂存与凭据字面量扫描通过；未纳入 `.workbuddy/`、数据库、CAS 或真实素材。

## PB04 边界

- 重新执行最终门禁并检查打包清单；Docker daemon 本机不可用，镜像构建由 PocketBay 完成。
- 旧管理员弱口令必须在上线前轮换；新值不进入对话输出、源码、归档或部署日志。
- 新建部署 session 后展示并打开完整 pairing URL；授权前不得上传。
- 只有 Agent 返回 `next_action=done` 且 `project_status=running`，并完成线上 health/registration/login/admin smoke，才能报告成功。
