# PB01 交接：生产安全加固

- Product commit：`7658bd0dce934d1e6ad3344c2c36d042e45a29b1`
- Gate：passed
- 后继阶段：PB02 加密备份与恢复

## 已交付

- API 和 Web BFF 仅接受配置中的单一 PocketBay Origin，不再接受任意 `*.pocketbay.app`。
- PocketBay 默认关闭公开注册；API、注册页面与登录页面入口保持一致。
- 管理员 bootstrap 仅在完整显式输入时运行；无输入时不动作，进程内变量在尝试后清除。
- Docker 与启动器固化生产 Origin 和注册关闭默认值，不包含任何凭据。

## 验证证据

- 定向 Vitest：4 files / 25 tests passed（Node 22）。
- 全量 Vitest：37 files / 790 tests passed（Node 22）。
- 根构建：2/2 tasks passed；Web 仅有 9 个既有 warning。
- Shell 验收：8/8 suites passed。
- 生产依赖审计：info/low/moderate/high/critical 均为 0，进程退出码 0。
- 暂存路径和凭据字面量扫描通过；未纳入 `.workbuddy/`、数据库、CAS 或真实素材。

## PB02 边界

- 复用现有 `LocalRecoveryService` 的一致性备份和隔离恢复，不在浏览器或日志中暴露口令。
- 恢复必须先隔离校验；激活采用显式确认和失败关闭，不能直接覆盖运行中的数据。
- 测试只使用仓库 fixture 和临时目录。
