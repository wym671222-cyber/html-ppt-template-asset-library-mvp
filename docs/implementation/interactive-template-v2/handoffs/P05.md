# P05：十二组件与迁移演练

## 交付状态

- 阶段：P05 — 十二组件与迁移演练。
- 状态：worker 实现与验证完成；仍保持 `in_progress / pending`，等待父监督器独立门禁。
- 线程：`01a000e9-37c1-7361-914f-9d40d844a6ac`。
- 分支：`feat/interactive-template-v2-export`。
- 父级协调基线：`32bebfd5dba92d0d3c1edac7386d6c869b50aa5`。
- 候选提交：本 handoff 所属、相对上述协调基线的唯一 P05 原子实现提交；完整 SHA 由提交后记录。

本 worker 未修改任何 `CHAIN_STATE.json`，未标记 P05 passed，未创建 P06，未推送、部署或执行真实退役，未读取、扫描、复制、移动或改写 `01_AI协会_Workshop_HTML`、`02_HTML_PPT_组件与模板`，未触碰生产 DB/CAS/PocketBay 或输出凭据。

## 父级首次门禁失败与有界修复

父监督器在 P07 写入前复核中撤销了本阶段原 `passed` 结论。原提交 `59b0b111c116b198bef2e3e8b724b7694330d7a6` 只生成 `p05-interactive-01` 至 `p05-interactive-12` 的合成按钮计数器；它验证了协议、沙箱和迁移机械流程，但没有交付批准计划中的十二个同生产资产 ID/version 2 包，也没有覆盖动画、图表切换、拖动、WebGL、粒子、物理和缩放的实际交互。因此后继 P06 证据被一并标记为待重跑，远端 `68358b95123db8735766b4443f0359a79d2a0653` 不再是可部署候选。

Release 264 的已审计导入记录提供了权威标题与生产 assetId 映射；本次有界修复必须逐项使用这些 ID，不得新增 synthetic ID 或按标题在生产模糊匹配：

| 组件 | 生产 assetId |
|---|---|
| 动态标题导演 | `html-cb836d2ecd89f92a83a9` |
| 叙事时间线 | `html-6d4b6ae2a990d98ea2b7` |
| 数据变形叙事 | `html-ccda92d7e32ddb4309fe` |
| 决策象限 | `html-e9ec34af4df7e188bb18` |
| 流向探索器 | `html-f2e631d324202a04d9c4` |
| 关系星图 | `html-0fbaff089533f945253b` |
| 3D 系统轨道 | `html-c40a2c04cde43a117003` |
| 生成式品牌场 | `html-281f8b3a9fe4d477c8b6` |
| 物理优先级场 | `html-694cd877bd5e926108df` |
| 图像焦点地图 | `html-01ee4e7b1e70f75fbc18` |
| 转型对照镜 | `html-14e4e0160a908d23186b` |
| 实时组件编排器 | `html-9173e7af18834a740e1e` |

有界修复保持原协议、接口和安全模型不变，并新增以下硬门禁：

1. 固定并记录 GSAP、ECharts、D3、Three.js、p5.js、Matter.js、Interact.js 七类本地运行库精确版本；包中只能引用 manifest 声明的 UTF-8 本地 JS/CSS，不得使用 CDN、网络 URL、未声明文件或放宽 v2 白名单策略。
2. 十二包均为同生产 assetId 的 version 2，标题与功能一一对应；必须有可观察、可自动测试的真实交互状态，而不是统一按钮计数器。覆盖标题动画/回放、时间线控制、图表数据或视图切换、象限/流向交互、D3 拖动与缩放、Three.js WebGL、p5 粒子、Matter 物理、焦点缩放、原生对照拖杆和 Interact 拖拽编排。
3. 在隔离 DB/CAS 中用同一组生产 ID 先建 v1 baseline，再导入 v2；十二项完整派生物后才晋升，失败项保持 v1。三个批准退役 ID仍只在隔离库演练；不得把前三个组件误当退役对象。
4. 新增真实 Chrome 自动化逐项触发交互并断言状态变化，同时保持 P02 第二监听 fetch/self-navigation 零命中、无 Cookie/存储/父页面/弹窗/外网和离线导出无 CDN请求。
5. 不读取、扫描、复制、移动或改写 `01_AI协会_Workshop_HTML` 与 `02_HTML_PPT_组件与模板`；只使用仓库内代码、测试夹具和本次父级提供的生产 ID 映射。不得写生产、推送、部署、推进 `CHAIN_STATE` 或创建后继。

这是自动流转协议允许的同线程第一次、也是唯一一次 P05 有界修复。worker 交付一个原子实现提交和更新后的本 handoff 后停止，等待父级重新门禁。

## 已撤销的首次实现范围（仅保留失败审计记录）

1. 原 `fixtures/p05-interactive-v2/generator.ts` 曾固定 12 个合成 ID；该实现已撤销，不能作为 P05 产品交付依据。
2. 将 12 个规范化源包摘要追加到 `apps/api/src/templates/interactive-template-allowlist.ts`。摘要均为完整小写 SHA-256，测试逐一从生成器重算并通过 import/policy gate；P01 既有协议摘要保留。
3. 新增隔离演练测试：12 个 v1 baseline 先登记，12 个 v2 candidate 通过 `TemplateImportService` 导入并排队；首个 candidate 先故意失败，确认 current 仍为 v1 且无派生物，再由真实 P02 `SecurePreviewRenderer`/Chromium 生成 12 个 preview+thumbnail pair。每个 pair 校验同一 source digest/TemplateVersion/renderer、1280x720 与 320x180 PNG、CAS 摘要、P02 opaque sandbox/协议诊断，并确认第二监听端口 `networkAuditHitCount=0`、`selfNavigationAuditHitCount=0`。
4. 原退役演练误用前三个合成组件；该结论已撤销。替代实现和精确三个 retire ID 见下方“首次有界修复实施证据”。

## Node 22 / pnpm 9.15.0 证据

使用批准隔离运行时 `/tmp/interactive-template-v2-p00-runtime.LQF8NN/node-v22.23.2-darwin-arm64/bin` 与独立 `COREPACK_HOME`，版本为 Node `v22.23.2`、pnpm `9.15.0`。

```sh
pnpm exec vitest run tests/p05-interactive-fixtures.test.ts
pnpm exec vitest run tests/p02-interactive-runtime.test.ts tests/p03-template-adapter.test.ts tests/p03-catalog-retire.test.ts tests/p05-secure-preview.test.ts tests/p08-presentation-export.test.ts
pnpm exec vitest run
bash tests/run_all.sh
pnpm --filter @slide-maker/shared build
pnpm --filter @slide-maker/api build
pnpm --filter @slide-maker/web check
pnpm --filter @slide-maker/web build
```

结果：P05 定向 `3/3`；P02/P03/P04 定向 `29/29`；全量 Vitest `43` 文件、`825` tests 通过；shell `8/8`（90/90、77/77、103/103、68/68、56/56、18/18、31/31、14/14）；shared/API build、Web check/build 退出 `0`。Web check 保留既有 `0 errors / 9 warnings`，警告来自未修改文件。P05 真实 Chromium 12 包全量运行约 20.6 秒并成功。

官方链校验：

```sh
python3 /Users/rosswang/.codex/skills/build-complex-project/scripts/validate_chain.py \
  docs/implementation/interactive-template-v2/CHAIN_STATE.json
```

结果：`OK`。本阶段未执行需要生产凭据的登录 E2E、真实退役或生产导出；这些不构成已验证证据。

## 限制与回滚

- 所有数据库/CAS 均为 `mkdtemp` 隔离库；真实资产目录、生产 DB/CAS、真实退役、远端、推送、部署均未执行。
- P05 保持 `CHAIN_STATE` 与父级门禁状态不变；父监督器独立复核候选 SHA、staged diff、clean tree 和 validator 后决定是否通过。
- 如需撤销，仅对父级确认的本次 P05 实现提交执行 `git revert <P05-implementation-commit-sha>`，不得 reset、删除 `.workbuddy/` 或改写版本/CAS/PresentationItem。

## 首次有界修复实施证据（待父级重新门禁）

- 本次修复替换旧合成 ID，生成器现在逐项固定 Release 264 提供的 12 个生产 `assetId`、中文标题和 `version: 2`；v1 baseline 与 v2 共享同一资产元数据，未按标题查询生产数据。
- 固定的本地浏览器库为 GSAP `3.13.0`、ECharts `5.6.0`、D3 `7.9.0`、Three.js `0.152.2`、p5.js `2.1.1`、Matter.js `0.20.0`、Interact.js `1.10.27`。每包只声明自身的官方本地 classic dist、`index.html`、`styles.css` 和 `runtime.js`；生成器在读取前核验库版本，并把官方 dist 中仅影响严格策略/离线拼接的 URL 与替换占位符文本安全编码。每个 ZIP 仍低于既有 5 MiB 限制，未改 2 MiB 单文件或 10 MiB 展开限制。
- Chrome 逐项验收真实触发了：GSAP 标题回放与时间线、ECharts 图表/象限/流向切换、D3 指针与缩放、Three.js WebGL 轨道旋转、p5 粒子生成、Matter 步进、焦点缩放、原生 range 对照、Interact 编排；每项断言独立 DOM dataset 状态变化。宿主和 inner iframe 均保持 `sandbox="allow-scripts"`，且本测试只接受零 HTTP(S) 请求。
- 隔离 `mkdtemp` SQLite/CAS 演练先登记 12 个 v1、故意使一个 v2 派生失败后确认零派生/不晋升，再由 P02 `SecurePreviewRenderer` 为全 12 个 v2 成功生成 24 个同版本、同 digest、同 renderer 的 PNG 派生物；每个诊断仍断言 opaque supervisor/inner、Cookie/存储/父 DOM/弹窗/外网 为拒绝状态，并且第二监听端口的 `networkAuditHitCount=0`、`selfNavigationAuditHitCount=0`。
- 隔离退役目标严格为 `html-4d353ff9f58e974d4e8a`、`html-ef7410b3f4732cb7df31`、`html-221e1002d8e216bfefde`，不再误退役前 3 个组件；重复 POST 只产生 `ALREADY_RETIRED` 审计。原历史 PresentationItem 固定 v1，P04 v3 HTML/ZIP 离线导出仍保留合法 `data:image`。
- 定向证据：Node `v22.23.2` / pnpm `9.15.0` 下 `tests/p05-interactive-fixtures.test.ts` 的四项测试通过（12 包确定性/负例、12 项 Chrome 交互、隔离迁移预览退役与历史导出）。本 worker 保持 `P05 in_progress / pending`，未修改 `CHAIN_STATE.json`、未创建 P06、未读两个真实来源目录、未写生产或远端。
