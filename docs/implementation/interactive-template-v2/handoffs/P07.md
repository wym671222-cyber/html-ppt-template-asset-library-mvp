# P07：PocketBay 迁移与终局验收

## 交付状态

- 阶段：P07 — PocketBay 迁移与终局验收（终端阶段，不存在后继）。
- 状态：P07 生产恢复门禁失败并停止；不存在后继。
- 线程：`01a00120-bf9d-7950-a479-c15a213bed6d`。
- 分支：`feat/interactive-template-v2-export`。
- 父级协调基线：`68358b95123db8735766b4443f0359a79d2a0653`。
- P06 已通过提交：`66135d5add134da929cb2da7e4810bad340e9ba3`。
- P07 部署使用精确 P06 产品候选 `66135d5add134da929cb2da7e4810bad340e9ba3`；本 handoff 记录 Release 326 切换及随后 `/data` 备份完整性漂移，不新增产品候选。

本轮未修改产品源码，未标记 P07 passed，未创建后继。已执行计划内的写前审计、备份、精确候选部署和部署后只读复核；未执行模板导入、版本晋升或真实退役。

## 本轮生产执行结果

- 写前 PocketBay 控制面：项目 `html-ppt-template-asset-library`，Release 264，`done/running`。
- 写前目录：30 个活动模板，精确为 15 个 Workshop、12 个生产组件 v1、3 个批准 retire 目标；十二个组件 ID 与 P05 v2 包逐项相同。
- 固定历史验收目标：`presentation-b81c6fc5-7c77-48d8-bed0-d79182e642e1`，owner 为单一生产 user，revision `18`，4 个 PresentationItem 均固定到 v1。
- 写前恢复检查：7 migrations、90 objects、60 derivatives、1 presentation、0 exports；state SHA-256 `0c1ec65b1ac39b74ca919b6afc21030ad064d296c7b7e864e9119eaeb53efb98`，database SHA-256 `0ffa20e37181db0d0c21c860d4323ab26529973bdf38721e1ea8675da0c0b57a`。
- 当前服务器备份 `backup-1786771370298-f386009b-a51e-4c2a-88e1-fd135b7a41c4` 与上述 state 相同；manifest SHA-256 `ceb9e9e8f6c227b1e4a9e73649eec78133b1173aa21e5cd66ba8dcb0f5041310`，数据库哈希 before/after 相同，`quick_check=ok`、foreign keys 开启且 foreign-key check 为 0。
- 精确 P06 候选归档共 530 个 tracked files，禁止目录、`.env`、credential path、private key、PocketBay token 和 bootstrap value 扫描全部为 0；归档 SHA-256 `428f0e3d91776e5327371c6e2753fac91b07e73cc4ab4c0ea45ed60967dacb07`。
- PocketBay 第一次 P07 上传成功，从 active Release 264 切换到 Release 326；平台返回 `next_action=done`、`project_status=running`，无 failure stage。按官方唤醒表单恢复后 live/ready 为 200。
- 部署后业务目录仍为 30 active、0 v2；历史 presentation 仍为 revision 18 且 4 项全 v1。
- 部署后恢复门禁失败：`/api/recovery`、当前写前备份 manifest 和此前已验证备份 manifest 均返回 409 `Backup SQLite hash or size verification failed`；另一个旧备份目录返回 missing。该漂移发生在任何 v2 import/promotion/retire 之前。
- 因此 P07 按自动流转协议停止：部署写入 1、Release 切换 1；v2 导入 0、current 晋升 0、retire 0、PresentationItem 迁移 0、物理 CAS 清理 0。

## 写前身份与门禁结论

写前缺口已经全部通过当前生产证据闭合：项目/Release、health、管理员和 owner、30 项活动目录、十二项精确映射、三个 retire ID、固定历史 v1 PresentationItem、SQLite/CAS 数量、数据库哈希和备份 manifest 均已取得。没有使用标题模糊匹配，也没有用隔离 fixture ID 冒充线上 ID。

当前 blocker 不是身份缺口，而是 Release 326 数据同步后的备份完整性失败。恢复服务按设计 fail-closed；不得绕过 manifest 哈希、删除坏备份目录、把 409 降级为 warning，或用健康 200 代替恢复门禁。

## 当前停止边界

- 生产部署上传：`1`；Release 切换：`1`（264 → 326）。
- v2 import：`0`；current promotion：`0`；真实 retire：`0`；PresentationItem 迁移：`0`；物理 CAS 清理：`0`；恢复激活：`0`；版本回滚：`0`。
- 当前线上目录仍为 30 active，尚未达到终局 27；十二个组件仍为 v1，三个合同模板仍 active。
- 已下载的 `.pba` 保持原样；没有读取口令、Cookie、storage、token、密码或密钥。
- 没有读取、扫描、导入、复制、移动或改写 `01_AI协会_Workshop_HTML`、`02_HTML_PPT_组件与模板`（含 component-lab/images）。

## 解除 blocker 所需决策

1. 用户在 PocketBay 控制台登录后，只读确认 Release 326 和可选 Release 264 的程序/`/data` 回滚范围。
2. 若执行版本回滚，必须由用户在动作时明确确认；PocketBay 协议禁止 agent 自动触发数据时间点回滚。
3. 回滚后重新验证 Release 264 的 catalog、presentation、`/api/recovery` 和三个备份 manifest。若恢复门禁重新通过，保持线上旧版，不再次上传同一候选。
4. 另行批准架构修复，解决 PocketBay 版本同步会改写/遗漏备份 SQLite 文件的问题；候选方向必须保持 fail-closed，并迁移/兼容既有备份，不能物理清理 CAS 或覆盖原备份。
5. 只有架构修复在隔离数据上通过“部署前备份 → 版本同步 → 部署后 manifest/restore”回归并形成新的 P06 候选，才可重新进入生产部署、十二项 v2 晋升和三个 retire。

## 门禁与回滚

- P07 gate=`failed`，workflow=`blocked`；没有后继。
- 官方 chain validator 必须保持 `OK`；`.workbuddy/` 继续保持未跟踪、未读取、未暂存。
- Git 撤销仅能撤销本次状态文档提交，不能回退 PocketBay Release 或 `/data`。PocketBay 版本回滚是独立的生产数据恢复动作，必须由用户在控制台确认。

完整只读根因取证见 `../diagnostics/P07-pocketbay-backup-drift.md`。当前结论是“平台数据同步物理改写/遗漏备份文件”最符合证据但仍待控制台或平台日志确认；未据此实施产品或架构修改。
