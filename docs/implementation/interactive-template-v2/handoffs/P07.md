# P07：PocketBay 迁移与终局验收

## 交付状态

- 阶段：P07 — PocketBay 迁移与终局验收（终端阶段，不存在后继）。
- 状态：P07 已在 P06 通过后重新启动写前只读核验；下文保留的阻塞审计是上一轮现场快照，当前仍未获得任何生产写入授权证据。
- 线程：`01a00120-bf9d-7950-a479-c15a213bed6d`。
- 分支：`feat/interactive-template-v2-export`。
- 父级协调基线：`68358b95123db8735766b4443f0359a79d2a0653`。
- P06 已通过提交：`66135d5add134da929cb2da7e4810bad340e9ba3`。
- 当前 P07 尚无产品候选提交；本 handoff 只记录写前只读核验和阻塞条件。

本轮未修改产品源码，未标记 P07 passed，未创建后继，未写生产数据库/CAS，未执行迁移或真实退役，也未 push/deploy。

## 部署前只读核验结果

| 核验项 | 当前证据 | 结论 |
|---|---|---|
| 历史 PocketBay 目标 | 既有 PB04 handoff 只证明当时 `https://html-ppt-template-asset-library.pocketbay.app` 的 Release 235 曾成功运行；PocketBay STATUS 同时说明休眠层可能先返回 204。 | 这是历史证据，不等于本次 P07 的当前目标状态或写入授权。 |
| 公开入口与健康探针 | 首轮无 Cookie GET 对 `/`、`/login`、`/api/health/live`、`/api/health/ready` 均返回 `204`、0 bytes；2026-08-15T00:45:31+08:00 复测四个 URL 均为 SSL connection timeout、HTTP `000`。 | 未取得计划要求的 live/ready `200`，不能把休眠层或超时当作应用健康。 |
| 浏览器会话 | 只读检查 Codex in-app Chrome 时，现有受控标签和用户标签均为 0；没有可复用登录态。对目标 URL 的新标签导航 5 秒后仍为 `about:blank`，未出现可验证 UI；随后已清理标签。未读 Cookie、storage、密码或 token，未点击任何按钮。 | 没有经过认证的 owner/admin/session，也没有可见 catalog 或迁移控制面。 |
| PocketBay 控制面与主机入口 | 当前可调用工具中没有 PocketBay project-status、pairing、文件或命令入口；仓库没有本次目标 `.env`、admin bootstrap 配置或服务器目标配置。仅发现 SSH agent socket 的存在，未据此猜测主机或授权。 | 没有明确服务器/平台访问方式、当前项目会话、单 Owner 确认或父级批准的 SSH 目标；未执行 SSH。 |
| Git 发布状态 | worker 只读核验时远端尚无本分支；随后父级按第 7 条授权将精确 P06 候选 `68358b95123db8735766b4443f0359a79d2a0653` 推送并复核为 `origin/feat/interactive-template-v2-export`，P07 blocker 文档未推送。既有 `feat/pocketbay-production` 仍在批准基线 `a427b530e05d79ae00b1d7196081f4a03c06451b`。 | P06 候选已按授权发布；没有发布 P07 blocker 文档或把历史部署分支误认作本次候选。 |
| `/data`、SQLite、CAS 与回滚空间 | 因没有已授权目标数据入口，无法只读取得当前 Release、活动清单、SQLite `quick_check`/`foreign_key_check`、CAS 登记与哈希、部署前数据库哈希、备份空间或旧 Release 可回退性。 | 不满足“先备份与只读核验、再由父级确认写入”的前置条件。P06 临时 SQLite/CAS 证据不得冒充生产。 |

只读审计没有读取或扫描任何凭据值；这里只记录入口/风险类型与是否存在。公开 URL 是仓库既有非秘密部署地址。

## 生产身份与验收目标缺口

即使恢复目标访问，当前仓库证据仍不足以安全执行 P07 写入：

1. `fixtures/p05-interactive-v2/generator.ts` 明确声明十二个 `p05-interactive-01` 至 `p05-interactive-12` ID 是合成 fixture，且绝不读取真实目录。它们不是经生产盘点确认的十二个静态组件 `assetId`。
2. 项目基线记载生产曾有 30 个活动模板（15 Workshop + 12 静态组件 + 3 合同续签），终局要求 27（15 + 12）。若线上仍是该 30 项基线，把十二个合成 ID 当作新资产导入并只退役三个目标会得到 39，而不是 27；要安全达到目标，必须把每个批准 v2 包精确映射到既有十二个静态组件资产并在同一资产下晋升版本，或由父级提供另一份经生产核验的权威映射。仓库内没有这份 12 对 12 映射。
3. 用户批准的 v2.0 计划已明确给出三个精确退役 `assetId`：`html-4d353ff9f58e974d4e8a`、`html-ef7410b3f4732cb7df31`、`html-221e1002d8e216bfefde`。它们不是当前 blocker；在目标访问恢复后仍需由管理员会话核对其当前状态与审计，但 P05 测试退役的前三个合成 fixture 不能替代这三个生产目标。
4. 仓库没有指定用于终局回归的既有 v1 `PresentationItem`/presentation ID、owner 与固定 revision。未登录生产也无法核对其仍指向 v1；因此不能选择性执行或声称旧演示导出已在线通过。

上述缺口会直接影响不可逆的生产身份选择。根据 P03 的精确 assetId、幂等软退役与已有引用不迁移合同，worker 不得猜测、模糊匹配标题或用 fixture ID 替代。

## 已停止的生产动作

- 生产迁移：`0`；current 晋升：`0`；真实 retire：`0`；生产审计事件：`0`。
- 生产备份/恢复：`0`；数据库/CAS 写入：`0`；SSH：`0`；PocketBay 控制面写入：`0`。
- P07 push、deploy、Release 切换、反向代理/HTTPS/binding/持久化卷修改：全部未执行；父级此前仅按协议推送了精确 P06 候选，P07 文档未推送。
- 没有读取、扫描、导入、复制、移动或改写 `01_AI协会_Workshop_HTML`、`02_HTML_PPT_组件与模板`（含 component-lab/images）；只使用了仓库 tracked 文件与 P06 已批准证据。
- 没有输出任何凭据值，也没有把隔离 fixture 的 12 包、27 计数或恢复结果描述为线上事实。

因此当前无法诚实报告线上活动总数、十二个 current v2、三个 retired、生产备份哈希、真实 Chrome 交互或线上离线导出成功；这些均保持“未验证”，而不是失败后豁免。

## 解除 blocker 所需的最小授权包

父监督器在重新放行 P07 写入前，需要同时提供或确认：

1. 当前 PocketBay 项目的可用 pairing/control session 或明确服务器访问方式，并确认精确项目仍为上述域名对应的目标；不得在消息中发送密码、token 或私钥值。
2. 目标域名或 VPN 暴露、reverse-proxy/HTTPS/binding、持久化 `/data`、部署目录/项目、单 Owner 与 admin/session 安全模式的当前决策。
3. 十二个批准 v2 包到生产既有十二个静态组件 `assetId` 的逐项映射；三个批准 retire ID 已固定为 `html-4d353ff9f58e974d4e8a`、`html-ef7410b3f4732cb7df31`、`html-221e1002d8e216bfefde`。
4. 一个用于验收的既有 v1 presentation/PresentationItem、owner 与固定 revision 标识；不得迁移或自动升级它。
5. 只读访问恢复后，先取得 current Release、health live/ready 200、认证活动清单、版本/current 状态、SQLite/CAS 完整性、部署前哈希、备份空间与上一 Release 回滚点；把结果交父级复核并获得明确写入确认。

只有上述五项同时满足，后续 worker 才可按计划执行：写前备份 → 部署候选 → 十二项同资产 v2 导入/派生物验证/晋升 → 三项精确软退役 → 27=15+12、旧 v1 与新 v2 的 Chrome/离线 v3 验收 → 回滚点复核。任何单项失败都必须停止且不继续后续写入。

## 门禁与回滚

- P07 gate 保持 `blocked`；本提交只保存纠正后的只读审计和 blocker，不是终局验收通过证据。
- 官方 chain validator 必须保持 `OK`；三份 `CHAIN_STATE.json` 均不在 P07 diff。
- 若父级撤销本次文档证据，只应对父级确认的 P07 候选执行 `git revert <P07-candidate-sha>`；不得 reset、删除 `.workbuddy/` 或改写 P00–P06 历史。

## 父级纠正追加

- 用户批准的 v2.0 计划已明确给出三个精确退役 ID：`html-4d353ff9f58e974d4e8a`、`html-ef7410b3f4732cb7df31`、`html-221e1002d8e216bfefde`。此前“退役 ID 缺失”的表述已删除；当前 blocker 仅保留目标访问/管理员与 `/data` 只读权限、health 200、十二项生产静态组件到 v2 包的权威映射、固定历史 v1 PresentationItem 及备份/回滚核验。
