# P00：执行链与失败基线

## 交付状态

- 阶段：P00 — 执行链与失败基线
- 状态：worker 交付完成，门禁仍为 `pending`
- 线程：`019fffb8-ed1b-78c2-bcb3-012d2db19b63`
- 分支：`feat/interactive-template-v2-export`
- 父级启动提交：`316d64ed1d8418386b7250086212d72b6ffba426`
- 模型合同：`gpt-5.6-luna / high`
- 候选提交：本文件所属的唯一 P00 原子实现提交，以父级复核后的 HEAD 为准

本阶段没有推进 `CHAIN_STATE.json` 门禁字段、没有创建 P01、没有修改产品源码、没有推送/部署，也没有接触数据库、CAS、远端或生产。

## 父级第一次有界修复（已完成）

父级独立复核要求补固定两项回归契约；本次只修改本 handoff、链 STATUS 和 `tests/p08-presentation-export.test.ts`：

1. 捕获 `PresentationExportRepository.create()` 的当前异常，并严格断言 `Error.message === "Verified template HTML contains an undeclared reference"`。
2. 在异常捕获后查询 `presentation_exports`，严格断言计数为 `0`，固定没有 partial/published export 记录。
3. 断言完成后重新抛出原异常，因此当前红测仍以原始精确错误退出 1，不掩盖产品失败。

本修复没有改变产品源码、运行时信任模型、数据/CAS、`CHAIN_STATE.json` 或阶段范围；它是父级允许的第一次有界修复。

## 范围内文件

本 worker 只修改了：

- `tests/p08-presentation-export.test.ts`
- `docs/implementation/interactive-template-v2/STATUS.md`
- `docs/implementation/interactive-template-v2/handoffs/P00.md`

保留并忽略既有未跟踪 `.workbuddy/`；`CHAIN_STATE.json` 未由本 worker 修改。

## 实现内容

1. 测试复用真实 P08 `PresentationExportRepository` fixture 和 v1 `html-template/v1` 包结构。
2. 新增一个合法 PNG 字节组成的真实 `data:image/png;base64,...` 静态快照，放入 v1 模板 `index.html`，重新计算并登记其真实 CAS/source digest。
3. 新增回归按“导出应成功”编写，因此当前产品行为会直接暴露错误；没有在测试中吞掉异常或修改产品逻辑。
4. 红测同时证明 P08 原有路径仍执行：7 个 P08 测试中 6 个通过、仅新增回归失败。

## 精确运行环境与命令结果

隔离临时运行时目录：`/tmp/interactive-template-v2-p00-runtime.LQF8NN`；该目录不属于仓库。Node 22.23.2 与 pnpm 9.15.0 由 Node 官方发行包和 Corepack 解析得到。仓库声明的 `.node-version` 为 `22`，`packageManager` 为 `pnpm@9.15.0`。

安装复现：

```sh
p00_runtime=/tmp/interactive-template-v2-p00-runtime.LQF8NN/node-v22.23.2-darwin-arm64
p00_node="$p00_runtime/bin/node"
p00_corepack="$p00_runtime/lib/node_modules/corepack/dist/corepack.js"
PATH="$p00_runtime/bin:$PATH" P00_COREPACK_HOME=/tmp/interactive-template-v2-p00-runtime.LQF8NN/corepack-home "$p00_node" "$p00_corepack" pnpm@9.15.0 install --frozen-lockfile
```

结果：退出码 `0`；lockfile up to date，依赖无需变更。

既有相关基线（修改前）：

```sh
PATH="$p00_runtime/bin:$PATH" P00_COREPACK_HOME=/tmp/interactive-template-v2-p00-runtime.LQF8NN/corepack-home "$p00_node" "$p00_corepack" pnpm@9.15.0 exec vitest run tests/p08-presentation-export.test.ts tests/pocketbay-adapter.test.ts tests/pocketbay-production-hardening.test.ts tests/pocketbay-recovery-encryption.test.ts tests/pocketbay-template-import.test.ts
```

结果：退出码 `0`；5 个测试文件、26 tests 全部通过。

新增红测：

```sh
PATH="$p00_runtime/bin:$PATH" P00_COREPACK_HOME=/tmp/interactive-template-v2-p00-runtime.LQF8NN/corepack-home "$p00_node" "$p00_corepack" pnpm@9.15.0 exec vitest run tests/p08-presentation-export.test.ts
```

结果：预期退出码 `1`；7 tests 中 6 通过、1 个新增回归失败。关键错误必须精确为：

```text
Verified template HTML contains an undeclared reference
```

调用链证据：`assertSafeStoredPackage` → `readPackage` → `PresentationExportRepository.loadSnapshot` → `PresentationExportRepository.create` → `tests/p08-presentation-export.test.ts`。

P08 原有测试与 PocketBay 入口的修补后复核：

```sh
PATH="$p00_runtime/bin:$PATH" P00_COREPACK_HOME=/tmp/interactive-template-v2-p00-runtime.LQF8NN/corepack-home "$p00_node" "$p00_corepack" pnpm@9.15.0 exec vitest run tests/pocketbay-adapter.test.ts tests/pocketbay-production-hardening.test.ts tests/pocketbay-recovery-encryption.test.ts tests/pocketbay-template-import.test.ts
```

结果：退出码 `0`；4 个测试文件、20 tests 全部通过。P08 原有 6 tests 在红测命令中全部通过。

## 第一次有界修复后的复核

P08 红测重跑命令：

```sh
PATH="$p00_runtime/bin:$PATH" P00_COREPACK_HOME=/tmp/interactive-template-v2-p00-runtime.LQF8NN/corepack-home "$p00_node" "$p00_corepack" pnpm@9.15.0 exec vitest run tests/p08-presentation-export.test.ts
```

结果：退出码 `1`；7 tests 中 6 通过、1 个新增回归失败；测试内已先断言精确 `Error.message` 和 `presentation_exports` 计数为 `0`，随后重新抛出原异常；控制台仍显示：

```text
Verified template HTML contains an undeclared reference
```

PocketBay 20 项绿测重跑命令：

```sh
PATH="$p00_runtime/bin:$PATH" P00_COREPACK_HOME=/tmp/interactive-template-v2-p00-runtime.LQF8NN/corepack-home "$p00_node" "$p00_corepack" pnpm@9.15.0 exec vitest run tests/pocketbay-adapter.test.ts tests/pocketbay-production-hardening.test.ts tests/pocketbay-recovery-encryption.test.ts tests/pocketbay-template-import.test.ts
```

结果：退出码 `0`；4 个测试文件、20 tests 全部通过。

## 负面证据与限制

- 本阶段没有放宽 `data:image` 安全策略，没有修改 `apps/api/src/**`、`packages/**`、`apps/web/**` 或其他产品源码。
- 没有运行会写入数据库/CAS/生产的命令；测试数据库与对象目录均由 fixture 创建在临时目录并由测试清理。
- 没有读取、复制、移动或改写 `01_AI协会_Workshop_HTML`、`02_HTML_PPT_组件与模板` 真实素材。
- 当前红测不是产品修复证据；它只固定 v1 合法 data-image 在现有 P08 导出链上的失败基线。P04 才负责修复导出协议。
- 当前 shell 默认 Node 24.18.0/pnpm 11.19.0 不符合仓库合同；P00 使用 PATH 前置的隔离 Node 22.23.2/pnpm 9.15.0 复核，避免 ABI/shebang 漂移。

## 回滚

父级确认候选 SHA 后，如需撤销本阶段，使用该唯一 P00 提交的反向提交：

```sh
git revert <P00-implementation-commit-sha>
```

不要使用 `reset --hard`、不要覆盖父级协调提交、不要删除 `.workbuddy/`。

## P01 后继只读集

P01 在父级门禁通过后，至少先读取并核对：

- `docs/implementation/interactive-template-v2/MASTER_PLAN.md`
- `docs/implementation/interactive-template-v2/STATUS.md`
- `docs/implementation/interactive-template-v2/CHAIN_STATE.json`
- 本 handoff 与 `docs/implementation/contracts/template-package-v1.md`
- `AGENTS.md`
- `packages/shared/src/template-package.ts` 及其验证测试
- `apps/api/src/templates/template-import.ts`
- `apps/api/src/templates/simulated-adapter.ts`
- `apps/api/src/assets/catalog-repository.ts`
- `apps/api/src/previews/secure-preview.ts`、相关 P05/PocketBay 导入测试

P01 仍须遵守 v1 兼容、v2 白名单和候选版本不提前晋升的阶段边界；不得把本 P00 红测误报为 P04 修复完成。
