# P06：全量候选与独立门禁

## 交付状态

- 阶段：P06 — 全量候选与独立门禁。
- 状态：worker 全量验证与审计完成；仍保持 `in_progress / pending`，等待父监督器独立门禁。
- 线程：`01a00110-abc1-74b3-b71b-e88f9a02bee6`。
- 分支：`feat/interactive-template-v2-export`。
- 父级协调基线：`8e0cda819eb976590baf56b21a2d806af9dc62a8`。
- P05 已通过提交：`59b0b111c116b198bef2e3e8b724b7694330d7a6`。
- 候选提交：本 handoff 所属、相对上述协调基线的唯一 P06 原子证据提交；完整 SHA 由提交后报告。

本 worker 未修改任何 `CHAIN_STATE.json`，未标记 P06 passed，未创建或推进 P07，未推送、部署或写生产。P06 没有发现需要修改产品源码的缺陷，因此候选只包含本 handoff 和同链 `STATUS.md` 的 worker 证据。

## 批准运行时与构建证据

所有正式命令使用批准隔离运行时 `/tmp/interactive-template-v2-p00-runtime.LQF8NN/node-v22.23.2-darwin-arm64/bin`、独立 `COREPACK_HOME` 和 pnpm `9.15.0`；实测版本为 Node `v22.23.2`、pnpm `9.15.0`、Vitest `3.2.4`。

| 门禁 | 结果 |
|---|---|
| shared build | `tsc` exit `0`。 |
| API build | `tsc` exit `0`。 |
| Web check | exit `0`；`0 errors / 9 warnings`，均为未修改文件中的既有 warning。 |
| Web build | adapter-node production build exit `0`；既有 Svelte/Rollup warning 保留。 |
| root/Turbo build | 首次运行时 Turbo 子进程误选 Codex fallback Node 24/pnpm 11，按原始 exit `1` 保留；仅在 `/tmp` 建立 Corepack pnpm 9.15.0 shim 后重跑，shared/API/Web `3/3` 成功。未重编依赖，未修改 manifest、lockfile 或依赖策略。 |

## 全量测试与真实 Chrome

| 命令/范围 | 结果 |
|---|---|
| `pnpm exec vitest run` | `43/43` files、`825/825` tests 通过。 |
| P02 real Chromium | opaque supervisor/runtime 会话通过；第二监听端口 fetch/self-navigation `0-hit` 测试通过。 |
| P05 real Chromium | 12 个 v2 包全量迁移、preview/thumbnail、退役与历史 v1 导出测试通过；约 21.3 秒。 |
| P08 real Chromium | v1/v2 ZIP 均在离线 Chrome 打开，P02 ready/runtime 边界保持；`10/10` P08 单测通过。 |
| P09 recovery Vitest | `10/10` 通过，覆盖备份、ZIP、隔离恢复、重复/篡改/穿越/软链/中断/权限负测和启动时激活回滚。 |
| `bash tests/run_all.sh` | `8/8` suites 通过（90/90、77/77、103/103、68/68、56/56、18/18、31/31、14/14）。 |
| `pnpm exec playwright test --config=playwright.p06.config.ts` | 启动前 3018/5175 均为 0 listener；真实 Google Chrome、1 worker，P06/P07/P08/P09 `12/12` 通过；退出后端口均释放。 |

测试只使用仓库内合成 fixture、`mkdtemp` 隔离 SQLite/CAS 和本机回环端口；没有调用真实来源或生产环境。

## P09 独立备份/恢复实测

另行用 P03 仓库 fixture 在单次临时根创建 7 migration SQLite 和 1 个 CAS 对象，执行 `LocalRecoveryService` 的 inspect → backup → isolated restore，并在输出证据后删除临时根：

- source state SHA-256 前后均为 `6d585932b7d5d444976836b4a404d75cdab7484c641720a8d801b0b73bca6389`；source database SHA-256 前后均为 `5c484f29f464bfa1c5ac6fb557493d8e783ef4e91314fc32af9361a903109cdc`，`sourceUnchanged=true`。
- CAS 登记摘要与重新计算摘要均为 `a0639bf37a0f692006476efc25b54d0e5b5a235383433d399ee2899e1818e408`，1/1 匹配。
- backup manifest SHA-256 为 `2e12496e85c11982b5a2e45ab6212d580ea067a1b924cf2d052957bf50c2b73b`；manifest 记录 `quickCheck=ok`、`foreignKeys=true`、`foreignKeyCheckCount=0`、migration count `7`、object count `1`。
- isolated restore database SHA-256 为 `d619d7ee1a04816194723a5bf90ff920ff39c81ce058e7af20718a2b2c09317a`；回读 `quick_check=ok`、`foreign_keys=1`、`foreign_key_check=0`、migration count `7`、object count `1`，逻辑 state SHA-256 与 source 相同。

## 生产依赖审计

生产门按既有批准协议执行 `pnpm audit --prod --json`：真实 exit `0`，369 个 production dependencies；info/low/moderate/high/critical 均为 `0`，总计 `0`。没有 audit ignore、降阈值、`--force`、approve-builds 或无证据 override。

同时保留调用方要求的字面补充命令 `pnpm audit --json`：真实 exit `1`，572 个全依赖项中 metadata 计数为 info `0`、low `2`、moderate `5`、high `1`、critical `1`；8 条 advisory 只出现在 Vitest/Vite/esbuild/Turbo/cookie 开发工具图。相同锁文件的 `--prod` 结果为上述全零，因此当前没有 production dependency finding。父监督器若把无 `--prod` 的开发依赖扫描也定义为 P06 必须全零门，应据此保持 P06 未通过并另行批准依赖修复；本 worker 没有把该非零结果隐藏、豁免或擅自扩成依赖升级。

## 凭据、路径与边界扫描

扫描严格基于 `git ls-files` 的 529 个 tracked files；未纳入未跟踪 `.workbuddy/`，没有遍历仓库外 sibling 目录。只输出风险类型、路径和计数，没有输出匹配值：

- high-confidence token format：`0`；private-key marker：`0`。
- credential-like assignment：全 tracked tree 为 `22` 处、`6` 个路径，均位于 `e2e/p15-auth-frontend.spec.ts`、`tests/p13-auth-approval.test.ts`、`tests/p15-e2e-api.ts`、`tests/pocketbay-production-hardening.test.ts` 和两个 `tools/qa-walkthrough-workspace/.../outputs/run-qa.*` 测试/QA 路径；deployable source（`apps/`、`packages/`、`fixtures/`、`ops/`、`scripts/`）为 `0`。
- deployable source 中 absolute user path 为 `0`；两个禁止真实素材目录名为 `0`。
- P06 全程没有读取、扫描、导入、复制、移动或改写 `01_AI协会_Workshop_HTML`、`02_HTML_PPT_组件与模板`（含 component-lab/images），也没有访问生产 DB/CAS/PocketBay/服务器。
- `.workbuddy/` 开始与结束的目录 metadata 均为 `Directory|96|1786240475`；最终 untracked scope 回读只报告其中 1 个 pathname，未读取文件内容、未暂存、未改动。

## 验收、限制与回滚

- 官方 `validate_chain.py docs/implementation/interactive-template-v2/CHAIN_STATE.json` 由 worker 运行并须保持 `OK`；三份 `CHAIN_STATE.json` 均不在 P06 diff。
- P06 gate 仍为 `pending`。父监督器应从完整候选 SHA 独立复跑 production audit、关键测试、validator、提交范围与 clean tree，并裁决上述无 `--prod` 补充审计结果；worker 不自行宣告 passed。
- 未执行 push、远端同步、部署、真实退役、真实迁移、生产备份或任何 P07 操作。
- 如需撤销，仅对父级确认的本次 P06 证据提交执行 `git revert <P06-candidate-sha>`；不得 reset、删除 `.workbuddy/` 或改写 P00–P05 产品提交。
