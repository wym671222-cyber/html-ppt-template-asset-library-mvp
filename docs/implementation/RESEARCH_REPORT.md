# 同类项目研究记录

- 状态：用户明确豁免
- 日期：2026-08-09
- 问题：是否先比较或引入外部身份/后台项目作为实现基线。

## 用户决定

用户明确要求“不做研究，只根据明确审批门的要求修订计划”。因此同类项目比较不构成本计划的前置门，也不采用研究阶段产生的候选结论。

## 采用路线

- 在当前已完成 P01–P10 的仓库和 SQLite/CAS 架构上做干净的生产化扩展。
- 账号、会话、审批和 Owner 迁移按当前明确需求最小实现，不 fork、不复制、不引入候选项目代码。
- 旧 Lucia/CUNY 邮箱身份代码仅作为待移除的现状，不重新挂载。

## 依据边界

- 计划只使用当前仓库、当前线上只读诊断、已确认产品决策和官方安全规范。
- 未验证的外部项目、许可证、发布状态或实现细节不得在后续阶段被描述为已批准依赖。
- 若执行期间确实需要新增重大第三方基线或改变认证架构，必须暂停并重新走计划版本审批。

## 待决定事项

无；研究门由用户明确豁免，流程直接进入 planning。
