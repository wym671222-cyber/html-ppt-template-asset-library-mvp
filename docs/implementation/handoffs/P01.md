# P01：架构审计与改造 ADR 交接

## 阶段

- 阶段：P01 架构审计与改造 ADR
- 状态：**通过**
- 日期：2026-08-08
- 分支：`personal/asset-library-mvp`
- P00 起点：`d5f4d3e3586058c560a5c8ae2af97a4e67a639f6`
- 上游基线：`upstream/main` @ `15b1a2713894bcde36a848d997f51d67760b441c`
- P01 提交：本阶段原子文档提交（以本文件所在 HEAD 为准）

## 完成内容

1. 按 P00 指定入口完成只读架构审计，并追踪身份、权限/审批、协作、数据库、文件存储、预览/导出、网络与测试横切依赖。
2. 新建 `ADR-P01-local-owner-mvp.md`，接受“新本机产品 composition root + 旧 CUNY 能力不挂载”的最小路径。
3. 明确上游已经是 SQLite/Drizzle、本地 uploads，未发现 Postgres/S3；需要移除的是活动身份/协作/在线依赖，而不是虚构云数据迁移。
4. 明确固定 OwnerContext、loopback-only、版本化迁移、SHA-256 CAS、本地 Job/Worker、Playwright/Chromium 的职责和阶段。
5. 形成模块映射、目标数据不变量、前向迁移顺序、停止条件、禁改区以及 P02 只读入口/门禁。
6. P01 只修改实施链文档；没有实现 P02 或后续产品功能。

## 修改路径

- `docs/implementation/ADR-P01-local-owner-mvp.md`
- `docs/implementation/STATUS.md`
- `docs/implementation/handoffs/P01.md`

## 验证

| 命令/检查 | 结果 | 证据/备注 |
|---|---|---|
| `git status --short --branch`（审计开始） | 通过 | `personal/asset-library-mvp`，HEAD 为 P00 提交，工作树干净 |
| `git rev-parse HEAD` / `git rev-parse upstream/main` | 通过 | 分别为 P00 `d5f4d3e...` 与精确上游 `15b1a271...` |
| `git remote -v` | 通过 | 仅 `upstream`；未修改远程、未 push |
| P00 指定文件逐项读取，`rg` 横切引用审计 | 通过 | 识别 SQLite、Lucia、RBAC/approval、sharing/lock/presence、mutable uploads、外联 preview/export、无本地 Job 等事实 |
| `git diff --check` | 通过 | P01 文档无空白错误 |
| `git diff --name-only d5f4d3e...` 范围检查 | 通过 | 只有三份 `docs/implementation/**` P01 文档；无源码/配置/资产改动 |
| ADR 章节/关键边界静态检查 | 通过 | 模块映射、迁移顺序、禁改区、OwnerContext/localhost/SQLite/CAS/Job/Chromium 和 P01 结论均存在 |
| build/test | 未重复运行 | P01 无源码/依赖/配置变化；沿用 P00 已记录基线，P02 必须重新运行适用验证 |

## 门禁判断

| P01 要求 | 判断 |
|---|---|
| 上游架构审计 | 通过 |
| 最小改造 ADR | 通过 |
| 模块映射 | 通过 |
| 数据迁移顺序 | 通过 |
| 禁改区 | 通过 |
| 满足单 Owner localhost 目标边界 | 通过（决策层；实现由 P02-P05 分阶段验证） |

**P01 门禁通过。** 可严格按 MASTER_PLAN 创建 P02；不得跳到 P03 或把本 ADR 当作产品功能完成证据。

## 遗留风险与未验证项

1. API 未显式指定 hostname，localhost-only 尚未实现；P02 必须有 loopback 正/负向测试。
2. 身份/RBAC/协作检查广泛内联于 routes 和 Web；P02 必须证明旧路由未挂载、前端不再触发旧调用，不能只删除登录页。
3. 没有 committed migration；现有 `drizzle-kit push` 不满足可审计迁移。
4. 当前 previews/artifacts/exports 允许外联或脚本，P05 前不能用于安全预览验收。
5. 没有持久化 Job/Worker/CAS；Playwright Chromium 仅作为 E2E 依赖存在。
6. 未发现 Postgres/S3，但存在其他 AWS/AI/邮件/搜索依赖；后续只能在活动引用清零后做最小清理。
7. P00 未产生业务数据；若 P02 发现现有非临时 DB/上传文件，必须先备份并停止自动转换。

## P02 下一阶段只读入口与禁改区

### 只读入口

1. `docs/implementation/{MASTER_PLAN.md,STATUS.md,ADR-P01-local-owner-mvp.md}` 和本文件；
2. `AGENTS.md`、根/API package 配置、Drizzle 配置、`.gitignore`；
3. API `index.ts`、`env.ts`、DB index/schema；
4. auth/admin/deck-lock middleware 与 auth/admin/sharing routes（仅作隔离参考）；
5. decks/files/resources/preview/export/chat/plan/search 中的 auth/RBAC/外联调用点；
6. Web app layout/home/deck page、API client、auth store、share/presence 调用点；
7. Vitest/Playwright/shell test 入口。

### 禁改区

- 真实 `02_HTML_PPT_组件与模板` HTML/素材；
- P03 及后续模板、CAS/Job 行为、安全预览、目标 UI、汇报、导出功能；
- remote/push/发布/部署/域名/云环境；
- 未备份的 DB、uploads 和任何未知用户数据；
- 与 P02 目标无关的上游重构。

## P02 完成门禁

1. 固定 OwnerContext 与 loopback-only Web/API/Worker 有正、负向验证；
2. 旧 auth/admin/sharing/presence/lock/在线 provider 不在活动路由图；
3. 目标 SQLite schema、编号 migration、备份/迁移验证和关键不变量测试通过；
4. 无 User/Organization/RBAC/approval/Postgres/S3/Redis/云队列；
5. build、unit/shell、Web check 和适用 E2E 通过并记录环境限制；
6. 只修改 P02 必需路径，未接触真实素材或远程；
7. STATUS/P02 handoff 完整后才可判断是否创建 P03。

## 回滚/恢复信息

- P01 仅新增/更新三份实施链 Markdown，可通过 P01 文档提交审计或反向变更。
- 上游仍可用 `15b1a2713894bcde36a848d997f51d67760b441c` 精确定位；P00 基线为 `d5f4d3e3586058c560a5c8ae2af97a4e67a639f6`。
- 未创建或迁移数据库、对象、Job、模板、缩略图和导出物；没有运行清理或删除命令。
