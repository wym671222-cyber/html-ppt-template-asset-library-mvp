# P11：可复现构建与故障止险基线

## 阶段

- 计划：v2.0（已批准）
- 状态：父监督者独立验证已通过
- 分支：`feat/production-auth-hardening`
- 批准基线：`438d259c27a08e322de803cb4b67b143a60f2794`
- P11 提交：`8d3cc0d91d2e4292967b3fba80f3aff5c7ed5542`
- 后继控制：worker 未创建 P12；父监督者通过 gate 后已独立创建 `/root/p12_auth_core`

## 交付

1. 根 manifest、`.node-version`、CI 固定 Node 22 与 pnpm 9.15.0；Web manifest/lockfile提交 adapter-node，移除未使用的 auto/static adapter，adapter-node `build/` 输出已加入 `.gitignore`。
2. API 提供独立 liveness/readiness；production composition root 的 readiness 运行 SQLite `SELECT 1`。`APP_READ_ONLY` 只接受精确 `true|false`，开启后所有 API 写方法稳定返回 `503 APP_READ_ONLY`，GET/HEAD 不被应用层开关误伤。
3. Web 提供只允许 `live|ready` 的同源 health BFF，保留既有 catalog/PNG BFF 边界。
4. CI 触发覆盖所有 push/PR；旧 CUNY/Tailscale SSH workflow 已替换为无部署动作的 guard，危险的旧 `deploy-staging.sh` 已删除，结构回归同步验证“已退役”。
5. 提供待 G1 审批的临时 Caddy allowlist：health 与 catalog/PNG 仅 GET/HEAD，其他 API 统一 503；JSON access log 固定 0600、10 MiB、10 files、30 天，并附备份/validate/reload/回滚 runbook。
6. API 启动事件改为结构化 JSON，只记录 event/host/port/readOnly，不包含 Cookie、请求头、请求体或凭据。

## 当前验证证据

| 命令/检查 | 结果 |
|---|---|
| `CI=true npx -y pnpm@9.15.0 install --frozen-lockfile` | 通过；lockfile up to date，514 packages；未运行 `pnpm approve-builds`。宿主 Node 24.18.0 对已锁 Node 22 给出预期 engine warning。 |
| `node_modules/.bin/vitest run tests/p11-production-baseline.test.ts` | 4/4 通过；覆盖 live/ready 正负向、只读读/写正负向、env 严格解析、工具链/CI/部署/Caddy 静态契约。 |
| `P05_CHROMIUM_PATH=... P06_CHROMIUM_PATH=... node_modules/.bin/vitest run` | 29 files、741 tests 全通过，只使用仓库 P03 fixture。 |
| `bash tests/run_all.sh` | 8/8 suites 全通过；结构测试现验证旧部署入口已退役。 |
| shared/API TypeScript | `tsc --noEmit -p packages/shared/tsconfig.json` 与 `tsc --noEmit -p apps/api/tsconfig.json --rootDir .` 通过。 |
| Web check/build | `svelte-check` 0 errors/9 条既有 warnings；Vite build 通过并明确使用 adapter-node。 |
| 根 `npx -y pnpm@9.15.0 build` | Turbo API/Web 2/2 tasks 通过；不再需要手工补装 adapter-node。 |
| staged-index 干净树 | `git checkout-index --all --prefix=/tmp/p11-clean.jFXsIF/` 后，在无旧依赖/输出的导出树 frozen install、根 build、P11 4/4 均通过。 |
| 差异/边界 | `git diff --check`、`git diff --cached --check` 通过；staged paths 不含 `.workbuddy/`、DB/CAS、凭据、CHAIN_STATE/DECISION_LOG 或 sibling 真实资产。remote 未修改、无 push。 |

## 精确环境限制

- 宿主当前为 Node `v24.18.0`，没有 corepack 或本机 Node 22；两次通过 npm 临时获取 Node 22 的探测卡在 `installArchSpecificPackage`，worker 只终止了自己启动的精确 PID 树。Node 22 当前由 `.node-version`、`engines.node=22.x` 与 GitHub CI `node-version: 22.x` 固定，但本机结果不能冒充 Node 22 运行证据。
- Docker CLI 存在，但 `docker info` 返回 daemon socket `/Users/rosswang/.docker/run/docker.sock` 不存在；本机也无 `caddy`/Go。因此 Caddy 容器 validate 在当前环境不可行，P11 只提交可重复的锁定 `caddy:2.10.0-alpine` validate 命令和静态契约测试，未启动 Docker Desktop、未连接服务器、未 reload Caddy。

## 只读与回滚边界

- P11 没有执行生产 DB/CAS 写入、迁移、服务重启、Caddy/PM2 变更、push 或 remote 修改。
- 应用回滚：revert 本 P11 原子提交。
- 临时 Caddy 配置仅在未来 G1 后人工应用；备份固定为 `/etc/caddy/backups/Caddyfile.pre-p11-<UTC_TIMESTAMP>`。回滚只用该备份恢复 `/etc/caddy/Caddyfile`，validate 通过后 reload，不触碰应用数据。
- `.workbuddy/` 继续保持用户自有未跟踪；未读取、扫描、导入、复制、移动或改写 sibling `02_HTML_PPT_组件与模板`。

## 父监督者验收入口

父监督者应独立核对：本提交恰好一个、`git status --short` 仅剩 `?? .workbuddy/`、上述定向/构建证据可复现、`.github` 无旧主机/SSH secret 路径、`CHAIN_STATE.json` 与 `DECISION_LOG.md` 未被本提交修改。通过前保持 P11 `in_progress`，不得创建 P12。
