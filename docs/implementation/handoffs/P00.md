# P00：源码落地与基线交接

## 阶段

- 阶段：P00 源码落地与基线
- 状态：**通过**
- 日期：2026-08-08
- 分支：`personal/asset-library-mvp`
- 上游：`upstream` → `https://github.com/CUNY-AI-Lab/slide-maker.git`
- 默认分支：`upstream/main`
- 精确上游提交：`15b1a2713894bcde36a848d997f51d67760b441c`（2026-08-07，`Merge pull request #9 from CUNY-AI-Lab/codex/slide-maker-check`）

## 完成内容

1. 克隆前只读确认固定目标目录不存在；已在固定目录克隆上游。
2. 已将 `origin` 重命名为 `upstream`；没有其他远程、没有 push。
3. 已创建并切换 `personal/asset-library-mvp`。
4. 父项目详细计划已更新为批准的路线 A：单 Owner、SQLite/Drizzle、本地内容寻址存储、本地 Job、Playwright/Chromium、localhost；明确移除 SSO、Postgres、S3、多人角色和审批，并保留未来替换边界。
5. 已写入 MASTER_PLAN、STATUS、handoff 模板和本文件。

## 工具链与验证

| 命令/检查 | 结果 | 备注 |
|---|---|---|
| `npx --yes pnpm@9.15.0 install --frozen-lockfile` | 通过 | 仓库 lockfile 未改；使用仓库声明版本 |
| `apps/api` 中 `require('better-sqlite3')` | 通过 | 本机 SQLite native binding 可加载 |
| esbuild binary `--version` | 通过 | 返回 `0.21.5` |
| `npx --yes --package pnpm@9.15.0 sh -c 'pnpm build'` | 通过 | Turbo：2/2 tasks successful；仅上游 warnings |
| `npx --yes --package pnpm@9.15.0 sh -c 'pnpm test'` | 通过 | Vitest 20 files/695 tests；shell suites 8/8 |
| `npx --yes --package pnpm@9.15.0 sh -c 'pnpm --filter @slide-maker/web check'` | 通过 | 0 errors、10 warnings |
| API 启动与 `curl http://localhost:3101/api/health` | 通过 | 临时 SQLite；响应 `{"status":"ok"}`；根路由亦为 `status: ok` |
| Web Vite dev server 与 `curl http://localhost:5173/` | 通过 | 返回 `HTTP 200`；服务随后已停止 |

## 上游基线告警（不由 P00 改动造成）

- Svelte 类型/构建检查报告 10 个 warning，主要是 a11y、CSS 兼容和一个初始值捕获提示；无 error。
- 构建报告动态导入 chunk 和大于 500kB chunk 的 warning；无 error。
- 临时启动未配置 AI、邮件或图片搜索凭据，因此这些可选上游功能不可用；健康接口正常。这不阻断 P01 的只读架构审计。
- 全局 pnpm `11.16.0` 的构建许可策略会改写 `pnpm-workspace.yaml` 并阻断后续命令；P00 已撤回其自动生成项。使用 `npx --package pnpm@9.15.0` 完成验证。
- localhost:3001 被未知既有进程占用；未操作该进程，API 改用 3101 通过健康验证。

## P01 只读入口

先按 `STATUS.md` 的 P01 列表读取；重点是：

- `apps/api/src/index.ts`：HTTP 入口、CORS/CSRF、路由挂载、健康端点；
- `apps/api/src/db/schema.ts` 与 `apps/api/src/db/seed.ts`：当前 SQLite/Drizzle 结构和种子边界；
- `apps/api/src/routes/resources.ts`、`decks.ts`、`export.ts`、`preview.ts`：资源、现有汇报、导出和预览能力；
- `apps/api/src/export/`：现有 HTML/ZIP 生成边界；
- `apps/web/src/routes/(app)/+page.svelte`、`apps/web/src/lib/components/resources/`、`apps/web/src/lib/components/editor/`：三栏 UI 与待替换入口；
- `packages/shared/src/{types,mutations,block-types}.ts`：跨端模型与 mutation 契约。

## 禁改区

- `02_HTML_PPT_组件与模板` 的真实 HTML/素材及其中除已更新计划文件外的任何文件；
- P00 基线以外的功能实现，尤其 SSO/角色/审批/云服务/真实资产导入；
- `upstream` 远程配置、远程推送、部署和任何外部发布。

## P01 门禁与后续

P01 必须先完成：架构审计、ADR、模块映射、数据迁移顺序与禁改区。仅在其结果明确满足 MASTER_PLAN 的单 Owner localhost 边界后，才创建 P02。

## 回滚/恢复

- P00 的可逆产品改动仅为本地分支上的文档提交；可通过该提交进行审计/反向变更。
- 上游原始 commit 可通过 `15b1a2713894bcde36a848d997f51d67760b441c` 精确定位。
- 健康检查使用 `mktemp -d` 下的临时 SQLite，未向仓库写入运行数据。
