# P16S：生产依赖安全修复

## 阶段结论

- 计划：v2.1（已批准）
- 批准基线：`5746fde602989f2ec75869cb58caa4f3674f61ef`
- 分支：`feat/production-auth-hardening`
- 状态：实现与工作区全量门禁完成；本 handoff 随唯一 P16S 产品提交交付，完整提交号由父监督者提交后回读并独立验收
- 后继控制：不得创建或实施 P17，不得申请或代替用户批准 G2

## 交付

1. `drizzle-orm` 升级到 patched `^0.45.2`，现有 better-sqlite3 schema、已提交 SQL migrations、journal、5/6/7 ledger 和 trigger contract 不变；`drizzle-kit` 升到 `^0.31.10`，不恢复 `push` 迁移路径。
2. `@hono/node-server` 固定到 patched `^2.0.5`，lock 实际解析 2.1.0；其 Node `>=20` 与 Hono 4 peer 均匹配项目目标，API/测试既有 `serve({ fetch, hostname, port }, callback)` 使用通过类型、构建与真实 Chrome 监听回归，无需源码适配。
3. Svelte/SvelteKit 固定到 `^5.56.8`/`^2.70.2`，让 production peer 图使用 patched devalue；其余直接产品声明只在原范围内解析到 patched 传递版本。
4. 未挂载的旧 CUNY 邮件模块改为 fail-closed 占位，四个历史函数稳定拒绝；`nodemailer`、`@aws-sdk/client-ses` 和类型包从 manifest/lock 移除，不再初始化 SMTP/SES 或读取邮件配置。
5. production lock 刷新到 patched lodash、xmldom、AWS XML、Hono、form-data、TipTap/linkify、PostCSS/nanoid 与 brace-expansion 图。
6. Drizzle optional peer 会自动安装最新 gel；gel 最新仍声明 `shell-quote ^1.8.1`，且没有可升级的直接父版本。因此根 manifest 仅加入 `gel>shell-quote=1.10.0` 的证据化窄 override。1.10.0 满足父范围，并高于两项 advisory 的最终 patched 门 `>=1.9.0`；不存在其他 override。
7. 新增 P16S 契约测试，锁住 patched Node-server/Drizzle/Kit、唯一 override、邮件依赖移除和全部退役邮件入口 fail closed。

## 当前验证证据

| 命令/检查 | 结果 |
|---|---|
| `CI=true npx -y pnpm@9.15.0 install --frozen-lockfile` | 最终 manifest/lock 在本工作区 frozen install 通过；宿主 Node 24 产生预期 Node 22 engine warning。 |
| `npx -y pnpm@9.15.0 audit --prod --audit-level high --json` | 真实 exit 0。JSON：369 production dependencies；info/low/moderate/high/critical 全部为 0。无 ignore、降阈值、`--force` 或无证据 override。 |
| production tree 回读 | Node-server 2.1.0、Drizzle 0.45.2、Hono 4.13.1、lodash 4.18.1、xmldom 0.8.13、devalue 5.9.0、form-data 4.0.6、postcss 8.5.26、nanoid 3.3.18、brace-expansion 2.1.4、shell-quote 1.10.0；Nodemailer/SES 不在树中。 |
| `node_modules/.bin/vitest run` | 35 files、779 tests 通过；含 P16S 2/2。 |
| `bash tests/run_all.sh` | 8/8 suites 通过。 |
| shared/API TypeScript | 两项 `tsc --noEmit` 通过。 |
| Web check/build | check 为 0 errors、9 个既有 warnings；adapter-node Web build 通过。 |
| `npx -y pnpm@9.15.0 build` | Turbo API/Web 2/2 通过。 |
| staged-index 干净导出 | 从精确暂存索引导出到独立 `/tmp` 目录并确认不含 `.workbuddy/`；fresh frozen install、production audit、P16S/P12/P14/P16 24/24、root build 2/2、P16 rehearsal 与 local preflight 全部通过。 |
| P12/P14/P16 定向 | 24/24 通过：5→7、6→7、7 no-op、wrong-ledger 无备份拒绝及 5/6/7 trigger verifier。 |
| `bash tests/p16-release-rehearsal.sh` | 非 root API/Web、隔离迁移、伪造 XFF、A/B 原子发布、累计 immutable 与 rollback 通过。 |
| `bash ops/workbuddy/preflight.sh --target local` | 仓库发布资产和 shell syntax 通过；Node 22、Caddy、systemd 与 Docker runtime 限制如实保留。 |
| P15 Chrome | 隔离 HTTPS 2/2 通过。 |
| P06–P09 Chrome | 10/10 通过。 |

上述数据库、CAS、服务、Chrome 与发布演练均使用仓库 P03 fixture、工具创建临时目录、隔离 SQLite/CAS 和空闲备用端口；没有接触实际或生产数据。

## 环境限制

- production audit 已无任何 severity 项；没有用 ignore、降阈值或 `--force` 达成关闭门。
- 本机 Node 是 24，不是生产锁定 Node 22；没有 Caddy、`systemd-analyze`，Docker daemon 不可用。本阶段不声称 Node 22/Caddy/systemd runtime 或任何生产现场验证。

## 回滚与边界

- 代码回滚：revert 唯一 P16S 产品提交，恢复其 manifest/lock、退役邮件占位、测试与文档；不触碰生产状态。
- 未读取、扫描、导入、复制、移动或改写 sibling `02_HTML_PPT_组件与模板`；未读取/修改 `.workbuddy/`。
- 未访问或修改实际/生产 DB/CAS、服务器、Caddy、systemd、PM2、remote；无 push、G1/G2、生产迁移、部署、`pnpm approve-builds`、audit ignore、降阈值或 `--force`。
- 未修改 `CHAIN_STATE.json`、`DECISION_LOG.md`，未创建 P17。

## 父监督者验收入口

1. 回读本提交相对 `5746fde602989f2ec75869cb58caa4f3674f61ef` 的唯一 commit 和精确 staged paths，确认 `.workbuddy/`、生产数据与 sibling 路径均不在提交。
2. 在提交树的干净导出中复现 frozen install、production audit 真实 exit 0 且所有 severity=0、P16S 契约和 root build。
3. 独立复跑至少 audit JSON、P16S/P12/P14/P16 定向、P16 rehearsal/local preflight，并核对 tracked clean。
4. 只有以上 gate 成立，才可把父级回读的完整提交 SHA 报告为 P17/G2 候选；仍不得创建 P17 或执行 push/生产操作，必须等待用户按完整 SHA 单独批准 G2。
