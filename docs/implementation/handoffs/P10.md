# P10：MVP 验收与本地交付

## 阶段

- 阶段：P10 MVP 验收与本地交付
- 状态：**通过，按计划停止**
- 日期：2026-08-09
- 分支：`personal/asset-library-mvp`
- P09 起点：`512485262633fc0569e76575fca20b75fd626a08`
- P10 提交：本文件、`STATUS.md` 与 `LOCAL_DELIVERY.md` 的原子提交（以 `git rev-parse HEAD` 为准）

## 验收证据边界

| 类别 | 当前验证 | 不可推导的结论 |
|---|---|---|
| 规范 | `MASTER_PLAN.md`、P03 `template-package-v1.md`、P01 ADR。 | 规范本身不替代运行结果。 |
| fixture-only | 唯一 P03 fixture 经过 P04 CAS、P05 Chrome preview/thumbnail、P06 catalog、P07 固定 TemplateVersion Presentation、P08 offline HTML/ZIP 和 P09 新目录恢复。 | 绝不等同于 sibling 真实 HTML/素材。 |
| 实际目标 | immutable/query-only 的 SQLite/FK/ledger/allowlist/SHA 检查，以及实际 backup/restore 回读。 | 当前业务表、CAS、P08 export 都为空，只能证明空目标非破坏性。 |

## 当前验证

| 检查 | 结果 |
|---|---|
| `P05_CHROMIUM_PATH=... P06_CHROMIUM_PATH=... node_modules/.bin/vitest run` | 28 files、737 tests 通过；P03-P09 fixture 旅程与 CAS/preview/catalog/Presentation/export/recovery 负向边界均重跑。 |
| `bash tests/run_all.sh` | 8/8 suites 通过。 |
| shared/API TypeScript | `tsc --noEmit -p packages/shared/tsconfig.json` 与 `tsc --noEmit -p apps/api/tsconfig.json --rootDir .` 通过。 |
| Web | `svelte-kit sync`、`svelte-check` 0 errors/9 条既有 warnings，`vite build` 通过（既有 a11y/chunk/adapter warnings）。 |
| P02 Chrome | `playwright test --config=playwright.p02.config.ts`：1/1，通过 Owner、loopback Host/Origin 和旧能力 404 回归。 |
| P06-P09 Chrome | `playwright test --config=playwright.p06.config.ts`：10/10，通过 fixture-only catalog、固定版本、离线 ZIP、隔离恢复、重复/不安全路由和 680px UI。 |
| 根 `pnpm build` | 预期受 pnpm ignored lifecycle approvals（better-sqlite3/esbuild）阻断；未批准依赖，自动 `allowBuilds` 占位已移除。 |
| 实际目标前后 | DB/WAL/SHM、backup manifest、restore report SHA-256 前后一致；DB、backup DB、restore DB 均 `quick_check=ok`、`foreign_keys=1`、`foreign_key_check=0`、5 migrations。实际 DB 的 content/asset/version/derivative/presentation/item/export/job 计数均为 0，CAS 根不存在。 |

## P10 门禁结论

1. MASTER_PLAN 六项不变量和 P02-P09 均有当前实测，而非单纯历史记录：**通过**。
2. fixture-only P04-P09 完整正向链路及缺失、篡改、路径穿越/符号链接/未声明文件、陈旧状态、并发/重复、中断、权限、loopback/Origin/Host、旧路由、离线与非执行模板负向链路：**通过**。
3. 恢复后 SQLite/ledger/FK、CAS、P05 派生物、P07 revision/item/version 与 P08 manifest/HTML/ZIP 全量重验：**通过（fixture-only 有对象旅程）**。
4. 实际 SQLite/CAS/export/backup/recovery immutable/query-only 检查、allowlist、SHA-256 与前后不变：**通过（实际为空）**。
5. unit/shell/shared/API/Web/build/Playwright：**通过；根构建审批环境限制已复测并记录，未改变依赖审批策略**。
6. 本地交付：`docs/implementation/LOCAL_DELIVERY.md` 已说明启动前只读检查、启动副作用、loopback 运行、回归命令、限制与恢复边界：**通过**。
7. 范围：未读取 sibling 真实资产；未修改 schema/migration、远程、凭据、依赖审批或产品功能；未 push/发布/部署，未实施 P11：**通过**。

## 停止条件

**P10 完成。不要创建 P11 或任何扩展阶段。** 若未来发生真实素材接入、非空实际库迁移、远程/部署或依赖审批，应由新的、单独授权的计划处理，不能反向修改本验收结论。
