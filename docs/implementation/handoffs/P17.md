# P17：生产迁移切换与终局验收

## 阶段结论

- 计划：v2.1（G2 已按完整 SHA 批准）
- 批准产品提交：`8d125d2d9afb213f449dbdc2d32c4939f5407441`
- 状态：`blocked`；GitHub personal 分支同 SHA CI 在原 run 与一次有界重跑中重复失败
- 生产效果：无；未连接或修改服务器、WorkBuddy、Caddy、systemd、PM2、生产 DB/CAS 或管理员状态
- 后继控制：P17 是终局阶段；未创建 P18

## 已执行的外部效果

1. 只向 `origin` 新建 `feat/production-auth-hardening` ref，精确 refspec 的源为获批产品 SHA，不是本地父级状态 HEAD。
2. feature CI run `31349030940` 在完整 SHA `8d125d2d9afb213f449dbdc2d32c4939f5407441` 上成功，覆盖 frozen install、build、type、tests、release rehearsal 和 Chrome E2E。
3. 现场回读 `origin/personal/asset-library-mvp` 仍为批准基线 `9c88b48aafd3bf2529cc31c5db9e346a915bf3aa` 后，使用普通非 force push 快进到同一获批产品 SHA。
4. 最终回读两个远端目标 ref 都精确指向获批产品 SHA。未 push upstream、tag、本地状态提交或额外提交。

## 阻断证据

- personal 分支 push 自动触发 CI run `31349153227`，head SHA 与批准产品 SHA 完全一致。
- attempt 1 在 `tests/p13-auth-approval.test.ts:354` 的 “includes the bootstrap CLI in the standard API TypeScript build graph and emits its runnable module” 测试超出 Vitest 默认 5000 ms；测试本身约 6.6 秒后被判失败。
- 只对 failed job 做了一次有界 rerun。attempt 2 在同一文件、同一测试、同一 5000 ms 门再次失败，测试本身约 6.5 秒；其余已经执行的 776 个测试通过，后续 release rehearsal 与 Chrome 因前置失败跳过。
- feature run 的一次成功不能覆盖 personal 最新 CI 的重复失败。P17 合同要求获批 SHA 的 CI 全绿，故 gate 失败；没有用继续重跑制造偶然绿灯。

## 安全停止与恢复

1. 停止点位于任何生产 preflight 或服务器访问之前；生产网站和数据没有因 P17 发生变化。
2. 远端分支已经合法快进，不能在没有新授权时 force 回退；保留现状最安全。
3. P17 允许修改范围不含产品测试/CI 源码，且当前 G2 只批准 `8d125d2d9afb213f449dbdc2d32c4939f5407441`。修正测试超时必然产生新产品提交和新候选 SHA，因此需要父监督者协调计划状态并取得新的精确 SHA 审批后才能重新发布。
4. 建议的最小恢复范围是只修复该 bootstrap build-graph 测试的确定性时限或隔离编译负载，复跑全量和两分支 CI；不得降低测试覆盖、跳过该测试或直接继续部署。

## 边界

- 未读取、暂存或修改仓库 `.workbuddy/`，只保留既有未跟踪目录。
- 未读取、扫描、导入、复制、移动或改写 sibling `02_HTML_PPT_组件与模板`。
- 未输出任何凭据值；未运行 `pnpm approve-builds`；未删除旧 release、旧 `/opt/slide-maker`、PM2 或任何备份。
- 未修改 `CHAIN_STATE.json` 或 `DECISION_LOG.md`；由父监督者独立 gate/reconcile。

## 父监督者复核

- 父级独立回读确认 `origin/feat/production-auth-hardening` 与 `origin/personal/asset-library-mvp` 均为获批 SHA `8d125d2d9afb213f449dbdc2d32c4939f5407441`。
- GitHub feature run `31349030940` 的 conclusion 为 `success`；personal run `31349153227` 的 attempt 2 conclusion 为 `failure`，失败日志明确定位到 `tests/p13-auth-approval.test.ts:354`，耗时约 6550 ms 后触发 5000 ms 超时，35 files 中 34 passed/1 failed。
- 阻断记录提交 `89329887425fb3e4d3bcfb4bb946ad48209fdd83` 仅含 STATUS 与本 handoff；tracked clean，仅用户自有 `.workbuddy/` 未跟踪。
- 父级 gate 结论：P17 `blocked`，workflow 不得 complete。生产服务器、WorkBuddy、Caddy、systemd、PM2、生产 DB/CAS 和管理员状态均未访问或修改。
