# P16T：CI 确定性修复与候选重建

## 阶段

- 计划：v2.2（已批准）
- 父级批准基线：`f331b98f5a3f9c7bcf3ed7a4c0b288398273bc37`
- 分支：`feat/production-auth-hardening`
- 状态：worker 门禁已通过；等待父监督者独立 gate/reconcile
- 候选提交：本 handoff 所在的单一 P16T 原子提交；完整 SHA 由提交后 Git 回读并交给父监督者核验
- 后继控制：worker 未创建或推进 P17；父监督者通过 gate 后只向用户报告新候选 SHA 并重新请求 G2

## 唯一实现

`tests/p13-auth-approval.test.ts` 中已有的 “includes the bootstrap CLI in the standard API TypeScript build graph and emits its runnable module” 单测仅增加 Vitest 第三参数 `15_000`。

- 保留真实 `node_modules/.bin/tsc` 调用。
- 保留 API `build=tsc`、`tsconfig.include` 包含 bootstrap 入口和实际 `dist/auth/bootstrap-admin.js` 产物断言。
- 未改其他断言、测试覆盖、产品源码、CI workflow、构建命令或全局测试配置。
- 未使用 skip、retry、全局 timeout、mock `tsc` 或删除编译负载。

## 当前验证证据

| 命令/检查 | 结果 |
|---|---|
| `node_modules/.bin/vitest run tests/p13-auth-approval.test.ts` 连续 3 次 | 每次 10/10 通过；目标真实编译约 1.0–1.2 秒。 |
| `node_modules/.bin/vitest run` | 35 files、779 tests 通过。 |
| `bash tests/run_all.sh` | 8/8 suites 通过。 |
| shared/API TypeScript | 两项 `tsc --noEmit` 通过。 |
| Web check/build | check 为 0 errors、9 个既有 warnings；adapter-node Web build 通过。 |
| `npx -y pnpm@9.15.0 build` | Turbo API/Web 2/2 通过。 |
| `npx -y pnpm@9.15.0 audit --prod --audit-level high --json` | 真实 exit 0；369 production dependencies，info/low/moderate/high/critical 全 0。 |
| `bash tests/p16-release-rehearsal.sh` | non-root API/Web、隔离迁移、代理负向、累计 immutable、原子切换和 rollback 通过。 |
| `bash ops/workbuddy/preflight.sh --target local` | 仓库发布资产与 shell syntax 通过；本机 Node 24、无 Caddy/systemd/Docker runtime 限制如实保留。 |
| P15 Chrome | 隔离 HTTPS 2/2 通过。 |
| P06–P09 Chrome | 10/10 通过。 |

## staged-index 干净导出

1. 精确暂存唯一功能文件，以 `git checkout-index --all --prefix=<isolated-temp>/` 导出索引；导出树不含未跟踪 `.workbuddy/`。
2. 独立树执行 `CI=true npx -y pnpm@9.15.0 install --frozen-lockfile`，通过。
3. 首次错误地在 Web sync 前执行全量 Vitest，3 个 Web suite 因 fresh tree 缺少生成的 `.svelte-kit/tsconfig.json` 在收集阶段失败；其余 32 files/756 tests 与 P13 均通过。该失败没有通过重跑掩盖，而是固定验证顺序。
4. 在同一 fresh tree 先执行 Web check/sync 后，P13 连续 3 次、全量 35/779、shell 8/8、shared/API type、Web/root build、production audit 全 0、P16 rehearsal/local preflight、P15 2/2 和 P06–P09 10/10 全部通过。
5. 本次提交随后只新增 STATUS 与本 handoff 两个交付文档；最终索引必须仍只含批准的 3 个路径，并由父监督者从候选提交再次独立复核。

## 边界与回滚

- 仅使用仓库 P03 fixture、隔离 SQLite/CAS、临时目录和备用端口；未触碰实际或生产 DB/CAS。
- 未读取、扫描、导入、复制、移动或改写 sibling `02_HTML_PPT_组件与模板`。
- 未读取、暂存或修改 `.workbuddy/`；它继续只是既有未跟踪目录。
- 未访问服务器或 WorkBuddy，未修改 Caddy/systemd/PM2，未修改 remote、push、G1/G2，也未运行 `pnpm approve-builds`。
- 未修改 `CHAIN_STATE.json` 或 `DECISION_LOG.md`，未创建 P17。
- 回滚仅需 revert 本 P16T 原子提交；远端两个分支继续指向旧 SHA，禁止 force 回退。

## 父监督者验收入口

父监督者应独立确认：批准基线到候选恰好一个 commit；变更路径严格为目标 P13 测试、STATUS 和本 handoff；测试差异只有单测级 `15_000`，断言/真实编译/覆盖均未漂移；关键门禁可复现；tracked worktree clean 且只有既有 `?? .workbuddy/`。通过前不得推进 P17、push 或部署；通过后只报告新完整 SHA 并请求新的精确 G2。
