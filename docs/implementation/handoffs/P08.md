# P08：HTML/ZIP 导出闭环交接

## 阶段

- 阶段：P08 HTML/ZIP 导出闭环
- 状态：**通过**
- 日期：2026-08-09
- 分支：`personal/asset-library-mvp`
- P07 起点：`e7ce3b5`
- P08 提交：本文件所属的原子提交（以 HEAD 为准）

## 完成内容

1. 新增 `PresentationExportRepository`，只接受当前 `expectedRevision` 与精确有序 `itemIds`；事务读取固定 Presentation snapshot，输出生成后在最终事务重读并比较指纹，持久化记录固定具体 TemplateVersion 而非 current version。
2. 每项重新验证 P03 canonical package、P04 source CAS 与 content/source SHA-256、slot schema/overrides，以及同 renderer 的 P05 preview/thumbnail CAS、PNG 媒体类型和尺寸。缺失、篡改、不一致、不可用或畸形输入在发布前拒绝。
3. 生成自包含静态 HTML：只对 P03 fixture profile 做作用域化 CSS 与受控 text/color slot 替换；不含外链、父目录、`file:`/data/blob/javascript 引用、script、iframe、form、事件属性或 cookie 能力。
4. 新增严格 stored ZIP writer/reader：路径为显式 allowlist 相对路径，local/central directory、UTF-8/stored 编码、CRC、重复/父目录/协议路径与全部文件 SHA-256 均回读校验。ZIP 只含 `index.html`、P05 thumbnail 和 embedded `manifest.json`。
5. audit manifest 固定 Presentation id/revision/name、item id/position/template_version_id、asset/version/contract、slot overrides、source/content 与 P05 derivative hashes，以及 HTML/ZIP/每个 ZIP 文件 hash、字节和媒体类型；list/manifest/html/zip 每次均重验 CAS 和 manifest/ZIP 一致性。
6. 活动 composition root 只增加最小本机 export 路由；Svelte 增加加载、空态、错误、冲突恢复、键盘焦点、Manifest/HTML/ZIP 链接和窄屏布局。模板 HTML 从不进入宿主 DOM、iframe、srcdoc 或 blob。
7. 新增 `0004_p08_presentation_exports.sql`，导出索引以 content object 外键、current revision insert trigger、媒体类型 trigger 和 update/delete append-only trigger 固定；成功不改变 Presentation/item/TemplateVersion/P05 派生物，失败无导出记录或 DB content object 半登记。

## 修改路径

- `apps/api/drizzle/{0004_p08_presentation_exports.sql,meta/_journal.json}`
- `apps/api/src/{app.ts,index.ts,db/{migrate.ts,schema.ts},presentation-exports/**}`
- `apps/web/src/{hooks.server.ts,lib/{presentations.ts,server/catalog-api.ts},routes/(app)/+page.svelte,routes/api/presentations/**}`
- `tests/{p02-foundation.test.ts,p06-asset-library.test.ts,p06-e2e-api.ts,p07-presentations.test.ts,p08-presentation-export.test.ts}`
- `e2e/p08-offline-export.spec.ts`、`playwright.p06.config.ts`
- `docs/implementation/{STATUS.md,handoffs/P08.md}`

## 验证

| 命令/检查 | 结果 | 证据/备注 |
|---|---|---|
| `P05_CHROMIUM_PATH=... P06_CHROMIUM_PATH=... node_modules/.bin/vitest run` | 通过 | 27 files、729 tests；P08 6 tests，P05 真实 Chrome 组件集成继续通过 |
| `bash tests/run_all.sh` | 通过 | 8/8 suites |
| shared/API `tsc --noEmit`；Web `svelte-kit sync`、`svelte-check`、`vite build` | 通过 | 0 errors；9 个上游既有 warnings，Vite 上游 chunk/adapter warnings 保留 |
| `playwright test --config=playwright.p02.config.ts` | 通过 | 本机 Google Chrome 1/1，固定 Owner/loopback/旧路由回归 |
| `playwright test --config=playwright.p06.config.ts` | 通过 | 本机 Google Chrome P06-P08 8/8；P08 3/3 含加载/空态/焦点、冲突/680px、ZIP 断网离线打开 |
| P08 offline audit | 通过 | ZIP 解包后 Chrome offline 打开；所有请求仅为解包目录内受控 `file:` 入口，0 HTTP(S)、0 cookie、0 script/iframe/form |
| 实际 DB migration | 通过 | 前置业务计数全 0；迁移前备份 `asset-library.1786209246469.pre-migration.db` SHA-256 `ca65c083e5478a0aab4e6b3bb877e78a875b8bdfef71d5958ab2fd2fc55f8dad`；迁移后 `7c4d335c80cbf99327f2dbde8790222ddf4c35f82358a7824702ab56f4ee7579`、5 migrations、table 1、trigger 4、`quick_check=ok`、FK check 0、业务表仍全 0；重复迁移后哈希不变 |
| 根 `pnpm build` | 环境阻断（已记录） | pnpm 10 ignored lifecycle scripts：better-sqlite3/esbuild；未执行 `approve-builds`，自动 `allowBuilds` 占位已精确移除 |
| 范围/差异 | 通过 | `git diff --check`；固定 `upstream`；无 sibling 真实素材操作、remote/push/发布/部署 |

## 遗留风险与未验证项

1. P08 静态 HTML 组装有意只支持 P03 fixture profile（无 CSS at-rule/复杂跨文档资源）；真实模板导入仍未授权，不能把本阶段 fixture 证明外推到 sibling 真实资产。
2. 根 Turbo 构建仍受桌面 pnpm 10 lifecycle approval 门禁阻断；本阶段没有改变依赖审批策略，直接 shared/API/Web checks 已通过。
3. 导出物是本机 CAS 与 SQLite 索引；P09 尚未执行跨目录备份、恢复、对象丢失/损坏恢复演练，不能据此宣称可恢复性已验收。

## P09 下一阶段只读入口与禁改区

P09 开始前完整读取 `MASTER_PLAN.md`、`STATUS.md`、本 handoff、P01 ADR、P03 package contract、AGENTS，以及 P04-P08 的 migration/schema、CAS、preview、catalog、Presentation、export、composition root、Web proxy/UI、unit/API/Playwright 入口。

P09 只允许做集成加固与本机备份/恢复演练：验证固定 Owner/loopback、SQLite、CAS、P05 派生物、Presentation revision 与 P08 export 在故障和新目录恢复后的边界。不得实施 P10 总验收或任何 P11；不得读取/导入/移动/改写 sibling `02_HTML_PPT_组件与模板` 真实素材；不得复挂旧 auth/admin/sharing/lock/presence/provider/preview/export/search 在线路由，或引入身份/RBAC/approval、Postgres/S3/Redis/外部 Worker、remote/push/发布/部署、依赖审批或凭据。

## P09 门禁

1. 有明确、可重复、非破坏的本机备份清单，覆盖目标 SQLite、P04 CAS、P05 派生物和 P08 导出索引/对象；备份前后逐对象/数据库 hash 可审计。
2. 在全新隔离目录完成恢复演练，`quick_check`、foreign keys/foreign_key_check、migration ledger、内容对象与导出 manifest/ZIP 哈希全部通过，且不修改原库/原 CAS。
3. 覆盖 DB/CAS/manifest/ZIP 缺失或篡改、备份中断、重复恢复、陈旧 revision 和只读/权限故障；失败有界、无半恢复、无覆盖已验证物。
4. 恢复后的 P06 catalog/P07 Presentation/P08 HTML/ZIP 核心旅程以 P03 fixture-only 的 unit/API/Playwright 通过，0 外网、0 宿主 cookie、旧能力仍 404。
5. 全量 unit/shell/shared/API/Web、适用 P02/P05-P08 E2E 与新增 P09 recovery E2E 通过；根 build 限制如实记录且不改审批策略。
6. 更新 STATUS/P09 handoff，显式暂存 P09 路径并形成一个 P09 原子提交；只有全部门禁通过才去重创建 P10，失败或不确定不得创建。

## 回滚/恢复信息

代码可回退到 P07 提交 `e7ce3b5`。实际 DB 的 P08 前向 migration 之前有备份 `apps/api/data/backups/asset-library.1786209246469.pre-migration.db`（SHA-256 `ca65c083e5478a0aab4e6b3bb877e78a875b8bdfef71d5958ab2fd2fc55f8dad`）；迁移后重复迁移前备份为 `asset-library.1786209246723.pre-migration.db`（SHA-256 `7c4d335c80cbf99327f2dbde8790222ddf4c35f82358a7824702ab56f4ee7579`）。不要删除或覆盖原 DB、P04 CAS、TemplateVersion、P05 派生物或 P08 导出物；P09 恢复必须使用隔离目标目录。
