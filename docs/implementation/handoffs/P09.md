# P09：集成加固与恢复演练交接

## 阶段

- 阶段：P09 集成加固与恢复演练
- 状态：**通过**
- 日期：2026-08-09
- 分支：`personal/asset-library-mvp`
- P08 起点：`d4871aa`
- P09 提交：本文件所属的原子提交（以 HEAD 为准）

## 完成内容

1. 新增本机恢复服务和命令行演练入口。清单版本为 `asset-library-local-backup/v1`，固定 SQLite 一致性快照、源数据库前后 SHA-256、逻辑状态、5 条 migration ledger/SQL SHA-256，以及所有受控 P04 CAS、P05 派生物、P07 Presentation/item 固定版本、P08 manifest/HTML/ZIP 的身份、角色、路径、字节和 SHA-256。
2. 备份和恢复均采用“隐藏临时目录完整写入与验证 → 原子 rename”。恢复只能进入从 backup id 派生的全新隔离目录；既有目标、重复恢复、中断或任何校验失败都拒绝并移除临时目录，绝不覆盖或改写原数据库、原 CAS、原派生物、原 export 或既有恢复。
3. 数据库检查覆盖目标 table allowlist、`quick_check`、foreign keys、`foreign_key_check`、migration ledger 与逻辑内容；CAS 从索引穷举后逐对象重验。P05 额外重验 preview/thumbnail pair、renderer/source、PNG 签名/尺寸和隔离诊断；P07 重验 revision、连续 position 与固定 TemplateVersion；P08 调用既有 repository 重验 manifest、HTML、ZIP、CRC/文件 allowlist 和全部哈希。
4. 路径参数不暴露给 API。backup id、SHA-256、相对文件均有长度和字符 allowlist；对象路径只能由 digest 派生。绝对路径、父目录穿越、反斜线、协议、符号链接、未声明文件、缺失/篡改对象、陈旧 source/manifest、只读/权限错误全部安全失败，诊断有界并遮蔽路径/凭据形态。
5. P02 活动 composition root 只增加恢复概览、创建备份、回读 manifest 和隔离恢复四类端点，继续使用固定 Owner、loopback Host/Origin、无 query 和 16KB JSON 限制。旧 auth/admin/sharing/lock/presence/provider/preview/export/search 在线路由继续 404。
6. Web 新增“备份与恢复演练”面板和同源受控代理，覆盖加载、错误、Manifest、备份、隔离恢复、键盘焦点、重复恢复及 680px 响应式；仍只渲染 JSON、受控链接和 P06 PNG，不执行模板 HTML，不使用 iframe/srcdoc/blob/可执行字符串。
7. P09 unit/API/E2E 仅使用仓库内 P03 fixture 与隔离 SQLite/CAS，完整生成 P04 CAS、P05 Chrome PNG、P06 catalog、P07 固定版本 Presentation 和 P08 离线 export，再执行备份/恢复与核心旅程重放；没有读取 sibling 真实资产。

## 修改路径

- `apps/api/src/recovery/**`
- `apps/api/src/{app.ts,index.ts,db/{migrate.ts,paths.ts}}`、`apps/api/package.json`
- `apps/web/src/{hooks.server.ts,lib/{recovery.ts,server/catalog-api.ts},routes/(app)/+page.svelte,routes/api/recovery/**}`
- `tests/{p06-e2e-api.ts,p09-local-recovery.test.ts}`
- `e2e/p09-local-recovery.spec.ts`、`playwright.p06.config.ts`
- `package.json`
- `docs/implementation/{STATUS.md,handoffs/P09.md}`

P09 没有新增或修改 schema/migration。

## 验证

| 命令/检查 | 结果 | 证据/备注 |
|---|---|---|
| `P05_CHROMIUM_PATH=... P06_CHROMIUM_PATH=... node_modules/.bin/vitest run` | 通过 | 28 files、737 tests；P09 8/8，P05 真实 Chrome 组件集成继续通过 |
| `bash tests/run_all.sh` | 通过 | 8/8 suites |
| shared/API `tsc --noEmit`；Web `svelte-kit sync`、`svelte-check`、`vite build` | 通过 | 0 errors；9 个上游既有 warnings，Vite 上游 chunk/adapter warnings 保留 |
| `playwright test --config=playwright.p02.config.ts` | 通过 | 本机 Google Chrome 1/1，固定 Owner/loopback/旧路由回归 |
| `playwright test --config=playwright.p06.config.ts` | 通过 | 本机 Google Chrome P06-P09 10/10；P09 2/2 覆盖键盘焦点、备份/恢复、源状态不变、重复恢复、680px 与不安全/旧路由 |
| fixture-only recovery replay | 通过 | 隔离恢复后 quick/FK/ledger、全部 CAS/P05/P07/P08 hash、catalog/PNG、固定版本 Presentation、manifest/HTML/ZIP 重验；P08 offline 回归继续为 0 HTTP(S)、0 cookie、0 active element |
| 根 `pnpm build` | 环境阻断（已记录） | pnpm 10 ignored lifecycle scripts：better-sqlite3/esbuild；未执行 `approve-builds`，自动 `allowBuilds` 占位已精确移除 |
| 范围/差异 | 通过 | `git diff --check`；无 schema/migration、sibling 真实素材、remote/push/发布/部署变化 |

## 实际目标预检与恢复演练

1. 实际数据库先通过 `file:...asset-library.db?mode=ro&immutable=1` 与 `query_only=ON` 只读预检：SHA-256 `7c4d335c80cbf99327f2dbde8790222ddf4c35f82358a7824702ab56f4ee7579`，`quick_check=ok`、`foreign_keys=1`、`foreign_key_check=0`、5 条 migration，asset/version/content/derivative/presentation/item/export 全为 0；无未知非临时数据，P09 不需要 migration。
2. 创建备份 `backup-1786210962800-392080b2-b4ae-4768-bbf2-5784848fbc3b`。manifest SHA-256 为 `a0062c968109f66a64a14e291fc4aaf0399cece6a88c23419eb64ec203d5e7b9`；snapshot DB SHA-256 为 `a753c907d4b1976c73e1ab6b29b453ea831b6c650854b630c217b1d6c90acf5c`；logical state SHA-256 为 `3ab83a561486b4f63c665c9af977bd605bdb3e3ba948d9d86285894502d4b573`。因实际业务表为空，清单中对象/派生物/Presentation/export 数均为 0；全数据能力由 fixture-only 恢复测试证明，二者不得混称。
3. 恢复到全新隔离目录 `restore-backup-1786210962800-392080b2-b4ae-4768-bbf2-5784848fbc3b`，恢复 DB SHA-256 同为 `a753c907d4b1976c73e1ab6b29b453ea831b6c650854b630c217b1d6c90acf5c`，`restore-report.json` SHA-256 为 `ddfd71ab9d98ab40080137e93707120470ce0373105909b7fdcae82dfaddd9de`；恢复库完整性、FK、5 条 migration/SQL SHA-256 全部回读通过。
4. 原 DB、WAL、SHM 前后 SHA-256 分别保持 `7c4d335c80cbf99327f2dbde8790222ddf4c35f82358a7824702ab56f4ee7579`、`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`、`fd4c9fda9cd3f9ae7c962b0ddf37232294d55580e1aa165aa06129b8549389eb`，业务计数仍全 0。备份/恢复产物位于 git-ignore 的固定本机数据目录，不进入提交。

## 遗留风险与边界

1. 实际目标当前没有业务对象，因此实际演练证明“空业务目标库的非破坏恢复”；P04-P08 全对象恢复由同一实现的 P03 fixture-only unit/API/Chrome 证据覆盖。P10 应明确保留这两类证据的边界。
2. 根 Turbo 构建仍受桌面 pnpm 10 lifecycle approval 门禁阻断；P09 没有改变依赖审批策略，直接 shared/API/Web 检查已经通过。
3. P09 是本机备份和隔离恢复能力，不是异机灾备、云存储、发布或生产部署；P10 不得把它扩展为 P11 或外部基础设施。

## P10 下一阶段只读入口与禁改区

P10 开始前必须完整读取 `docs/implementation/MASTER_PLAN.md`、`docs/implementation/STATUS.md`、本 handoff、`docs/implementation/contracts/template-package-v1.md`、`docs/implementation/ADR-P01-local-owner-mvp.md`、`AGENTS.md`，以及 P03 fixture、P04 CAS、P05 preview/thumbnail、P06 catalog、P07 Presentation、P08 export、P09 recovery、活动 API/Web composition root、schema/migration 与 unit/API/Web/Playwright 入口。

P10 只做 MVP 最终验收与本地交付，不扩展功能：逐条以当前可复现证据核验 MASTER_PLAN 全局不变量和 P02-P09；只用 P03 fixture 验收 P04 CAS、P05 preview/thumbnail、P06 catalog、P07 Presentation、P08 离线 export、P09 隔离恢复端到端旅程；覆盖失败/安全边界、实际 SQLite/CAS/export/backup 只读完整性、全量测试和交付文档。不得读取、扫描、导入、复制、移动或改写 sibling `02_HTML_PPT_组件与模板`；不得 push、发布、部署，不得实施 P11；不得复挂旧 auth/admin/sharing/lock/presence/provider/preview/export/search 路由，或引入身份/RBAC/approval、Postgres/S3/Redis、云队列/外部 Worker、远程配置、域名/nginx、依赖审批或凭据。

## P10 门禁与递归协议

1. MASTER_PLAN 全部全局不变量和 P02-P09 均有逐条、可复现、证据边界清晰的验收记录。
2. P03 fixture-only 的 P04-P09 正向端到端和缺失/篡改/路径/陈旧/并发/离线/旧路由等负向安全旅程通过；Web 继续不执行模板内容。
3. 实际 SQLite、CAS、export、backup 只读完整性通过；实际为空的范围如实记录，不得由 fixture 证据外推为真实素材验收。
4. 全量 unit/shell/shared/API/Web/build 与适用 Playwright 回归通过；环境限制如实记录且不得改变依赖审批策略。
5. 更新 `STATUS.md`、`docs/implementation/handoffs/P10.md` 与本地交付说明，显式暂存 P10 路径并形成单一 P10 原子提交，最终 `git status` 干净。
6. 任一门禁失败或不确定时留在 P10 修复并报告精确阻断；P10 通过后停止，不创建 P11 或任何扩展任务。P10 最终必须报告当前阶段 commit、验证摘要，并明确没有下一阶段 thread id。

## 回滚/恢复信息

代码可回退到 P08 提交 `d4871aa`。P09 未新增 migration；实际原数据库保持 P08 后 SHA-256 `7c4d335c80cbf99327f2dbde8790222ddf4c35f82358a7824702ab56f4ee7579`。不要删除或覆盖本机实际 DB/CAS、`recovery-backups`、`recovery-drills` 或 P08 以前的数据库备份；任何后续恢复验证仍必须进入新的隔离目录。
