# P07：购物车与汇报持久化交接

## 阶段

- 阶段：P07 购物车与汇报持久化
- 状态：**通过**
- 日期：2026-08-09
- 分支：`personal/asset-library-mvp`
- P06 起点：`c0bed82bc8bc719616d4386105019e9073010c1c`
- P07 提交：本文件所属的原子提交（以 HEAD 为准）

## 完成内容

1. 新增 `PresentationRepository`，固定 OwnerContext 下实现 Presentation 的创建、列表/读取、CAS 重命名、从 catalog 加入、复制、删除、排序及 schema 控制的 slot 覆盖。每个成功逻辑写入均在单 SQLite transaction 中验证 expected revision 并只加一。
2. 新增最小 `/api/presentations` 路由和同源 SvelteKit proxy；所有写 body 有 JSON/object/16KB 上限，所有 id/name/position/revision 输入有界，未挂载任何旧 route。
3. 加入 item 前在服务端重验 P06 资格：active asset/current version、verified/available、source object 一致、同 renderer 的 preview/thumbnail PNG pair。落库只保存具体 `template_version_id`。
4. 新增 `0003_p07_presentation_items.sql`：移除 P02 的阶段性排序/删除限制，保留 position 唯一、版本固定和 slot trigger；slot trigger 改为比较 schema slot object 的 `id`，避免误拒绝合法声明槽。
5. Web 保留 P06 三栏与 PNG-only 边界，在中栏下方增加本机汇报购物车：创建、重命名、加入、上/下移、复制、删除、title 覆盖、revision 显示和冲突重载。模板 HTML 从未进入 DOM、iframe、srcdoc、blob 或可执行字符串。

## 验证

| 命令/检查 | 结果 |
|---|---|
| `P05_CHROMIUM_PATH=... P06_CHROMIUM_PATH=... node_modules/.bin/vitest run` | 26 files、723 tests 通过；P07 4 tests，含 CAS、固定版本、排序、负向 slot/API/migration |
| `bash tests/run_all.sh` | 8/8 suites 通过 |
| API `tsc --noEmit`；Web `svelte-kit sync`、`svelte-check`、`vite build` | 通过；svelte-check 0 errors、9 个上游 warnings，Vite 上游 a11y/chunk/adapter warnings 保留 |
| `playwright test --config=playwright.p06.config.ts` | 真实本机 Google Chrome P06+P07 5/5 通过；使用隔离 DB/CAS 和唯一 P03 fixture |
| 实际 DB migration | 迁移前目标表、业务计数 0、无未知数据；备份 `asset-library.1786207558184.pre-migration.db` SHA-256 `a501811f7b328fd36799049a5b2596b84d385f45b036ff6f9b77720aaab1bba9`；迁移后 `ca65c083e5478a0aab4e6b3bb877e78a875b8bdfef71d5958ab2fd2fc55f8dad`、`quick_check=ok`、FK check 0、4 migrations、业务计数仍 0；实际重复 migration 再次备份并保持相同哈希 |
| 根 `pnpm build` | 已执行但被 pnpm 10 ignored lifecycle approval 阻断；没有批准依赖，自动生成的 workspace approval 占位已移除 |

## P08 入口与限制

P08 只能基于固定 Presentation revision 和固定 TemplateVersion 重建 HTML/ZIP manifest、来源 hash、离线打开与失败诊断；不得让已有 item 漂移到 current version。继续使用 P03 fixture、P04 CAS、P05 受控资源，不读取真实 `02` 素材。

不得提前实施 P09 恢复演练、P10 验收或任何 P11；不得重新挂载旧 auth/admin/sharing/lock/presence/provider/preview/export/search/在线路由，或引入身份/RBAC/approval、Postgres/S3/Redis/外部 worker、remote/push/发布/部署。

## 回滚/恢复

代码可回退到 P06 提交 `c0bed82bc8bc719616d4386105019e9073010c1c`。实际 DB 的 P07 前向 migration 已有可验证备份 `apps/api/data/backups/asset-library.1786207558184.pre-migration.db`；不要删除或覆盖 P04/P05 CAS、TemplateVersion、Job 或 PNG 派生物。P08 前若需实际 DB 变更，仍先预检、备份、哈希并在发现未知数据时停止。
