# P03：模拟模板与适配器交接

## 阶段

- 阶段：P03 模拟模板与适配器
- 状态：**通过**
- 日期：2026-08-08
- 分支：`personal/asset-library-mvp`
- P02 起点：`d696504eda2780b5d844cc78413bbc0229ffbc23`
- P03 提交：本阶段原子提交（以本文件所在 HEAD 为准）

## 完成内容

1. 建立 `html-template/v1` 契约：显式 manifest/files、包内 `index.html` entry、安全相对路径、text/color slots 和 `data-template-slot` 绑定。
2. 新建仓库内模拟包 `fixtures/p03-simulated-template/`；它是唯一 P03 输入，不来自任何真实素材目录。
3. 建立只读模拟适配器：读取 manifest 声明的文件，拒绝越界路径、重复/不安全文件、脚本、外部 URL 和未声明 slot，返回 `TemplateAsset`/`TemplateVersion` 形状的内存对象。
4. 使用排序后的 manifest 与文件内容计算确定性 `sourceDigest`；P03 不写 DB、不写 CAS，`contentObjectDigest` 保持 `null`。
5. 新增单元测试和契约文档；未挂载 API 路由，也未进入预览、UI、Presentation 或导出流程。

## 修改路径

- `packages/shared/src/template-package.ts`
- `packages/shared/src/index.ts`
- `apps/api/src/templates/simulated-adapter.ts`
- `fixtures/p03-simulated-template/{manifest.json,index.html,styles.css}`
- `tests/p03-template-adapter.test.ts`
- `docs/implementation/contracts/template-package-v1.md`
- `docs/implementation/STATUS.md`
- `docs/implementation/handoffs/P03.md`

## 验证

| 命令/检查 | 结果 | 证据/备注 |
|---|---|---|
| `node_modules/.bin/vitest run` | 通过 | 22 files、703 tests |
| `bash tests/run_all.sh` | 通过 | 8/8 suites |
| `node_modules/.bin/tsc --noEmit -p packages/shared/tsconfig.json` | 通过 | shared 类型通过 |
| `node_modules/.bin/tsc --noEmit -p apps/api/tsconfig.json` | 通过 | API 类型通过 |
| `apps/web/node_modules/.bin/svelte-check --tsconfig ./tsconfig.json`（cwd `apps/web`） | 通过 | 0 errors、9 个既有 warnings |
| `git diff --check` | 通过 | 无空白错误 |
| sibling 边界审计 | 通过 | P03 代码、fixture、契约和测试无 `02_HTML_PPT_组件与模板` 路径引用 |

## 数据与安全边界

- 没有新增数据库记录、SQLite migration、CAS 对象、Job 或运行时服务。
- 没有读取、扫描、导入、复制、移动或改写 `/Users/rosswang/Desktop/HTML - PPT/02_HTML_PPT_组件与模板`。
- 没有修改远程、push、部署、云环境或凭据。
- P03 的 HTML 检查是包契约校验，不是 P05 的 Chromium 安全预览；网络隔离、cookie、父目录和截图门禁仍待 P05。

## P04 入口与限制

P04 可以基于本交接实现 CAS、资产目录 repository、SQLite Job 和本机 Worker，但必须继续使用本 P03 输出的 `sourceDigest`/slot schema 边界。P04 不得把模拟适配器当作真实素材导入器，也不得提前实现预览、UI、汇报旅程或导出。

## 回滚/恢复信息

本阶段仅新增代码、文档和 fixture；回滚可使用 P02 提交 `d696504eda2780b5d844cc78413bbc0229ffbc23`，不会删除本机 `apps/api/data/` 或任何用户数据。
